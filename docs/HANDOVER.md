# arb-bot 引き継ぎ資料(2026年9月20日時点)

## 目的と方針(オーナーが決めたこと)
- DEX内で完結する原子的裁定bot。フラッシュスワップ+自作コントラクト。CEX裁定は対象外
- オーナーはiPhoneのみで操作。撤退は考えておらず、実測に基づいて段階的に改善し利益を伸ばす
- Ethereum本体は対象外。チェーンは「同じロジックで全部」ではなく、チェーンごとに攻め方を作って1つずつ増やす
- 推測で決めない。アドレス・手数料・ガス代・イベント識別子は必ず実測または公式資料で確認する
- 修正は「実装が目的」にならないよう、利益への効果を先に説明する
- 作業は1つずつ提案する

## 稼働環境
- GitHub: mako00121200-ops/arb-bot(main にMergeするとRailwayが自動デプロイ)
- Railway: プロジェクトID f9a69c7e-b52c-4e06-bd1b-9863019619bc / サービスID d47495c6-84bb-4228-9f87-bc2f2f9a9ddb(secure-amazement)/ 環境 production
- ダッシュボード: https://secure-amazement-production-5364.up.railway.app
- RPC: Chainstack Growth(月2,000万リクエスト、Extra usageオフ=超過で停止)。PolygonのみChainstackのHTTP+WSS。Arbitrum/Avalancheは公開RPCの定期読み直し
- 稼働チェーン: ACTIVE_CHAINS=polygon,arbitrum,avalanche,optimism(2026年9月19日に Optimism を再開。Base は停止、設定は残してある)。ENABLE_FORK_QUOTER=polygon,optimism,avalanche(avalanche は2026年9月20日の再デプロイ後に追加)
- botウォレット: 0x9D926340a8F14D3351470997684bD8C4767131f1
- コントラクト(**全チェーンがガス削減版、2026年9月20日に再デプロイ**。起動ログの
  `[コントラクト] <チェーン> …: ガス削減版(flags) と判別しました` で4チェーンとも確認済み):
  - Polygon **0xd0Fae44b77833744faFfA012082ba001DfB7E0fF**
  - Avalanche **0x3bCdCC60b0d1A8a98152A08985dF87C47f2e4Cc9**
  - Optimism **0x43e0536aA8F740253A7f6580565695c7F7A3E9A7**(展開時に RPC が "already known" を返して
    台本は失敗扱いになったが、取引は送られていた。住所は送信者と nonce=8 から計算し、起動時の確認で裏付けた)
  - Arbitrum **0xD2D45cC99AAe1AF7302b067d116fEEA8d7ceAca1**(Avalanche / Optimism の旧コントラクトと同じ住所だが別チェーンの別物)
  - 旧版(利益が残っている可能性あり、withdraw は所有者キーで直接呼ぶ): Polygon 0xB26722e1…(9月18日版)と
    0xD2D45cC9…(それ以前)、Avalanche 0xD2D45cC9…、Optimism 0xD2D45cC9…、Arbitrum 0x2139C149…
- Optimism の RPC/WSS は Chainstack(2026年9月19日に追加)。`OPTIMISM_RPC_URL` / `OPTIMISM_WSS_URL` に設定済み
- **コントラクトのガス削減版(2026年9月20日、`contracts/` を変更、同日に4チェーンとも再デプロイ済み)**。
  Leg の形が変わったが、bot は起動時と送信時に各チェーンのコントラクトへ `FLAG_V3()` を問い合わせて
  新旧を判別するので、版が混在しても動く。下の「コントラクトのガス削減」の節を参照
- 旧Polygonコントラクト 0xD2D45cC9… には利益が残っている(推定$0.5前後)。所有者キーは
  同じなのでいつでも回収できるが、withdrawを呼ぶ仕組みはまだ無い
- コントラクトの再デプロイは環境変数 RUN_MAINNET_DEPLOY=<チェーン名> で起動時に実行し、完了後に MAINNET_CONTRACT_ADDRESS_<チェーン> を設定して RUN_MAINNET_DEPLOY=false に戻す

## 現在の仕組み(2026年9月19日に書き直し。下の各節が経緯)

- V2の `Sync` / V3の `Swap` をWebSocketで受信し、メモリ上の地図で経路(2〜3段)を即判定
- **始点トークンは36種**。手書きの一覧に加え、V2で深さ$50,000以上のトークンを
  起動時に選んでV3プールを探す(`V3_DISCOVERY_TOKENS=40`)。価格は安定通貨から
  隣へ辿って逆算し、5分ごとに作り直す
- V3の見積もりは**価格表**(公式QuoterV2、フォークとAlgebraは自前の `quoteV3`)。
  ただし**作り置きはしない**(`QUOTE_TABLE_FILL_ALL=false`)。現在価格と深さで
  ふるいにかけ、**ガス代を引いて$0.01を超える候補が出た経路の段だけ**その場で
  作る。費用はプール数ではなく候補の数に比例する
- 価格表は作った時の価格つきで `/data` に保存し、起動時に**動いていない表だけ**戻す
- 価格表の作り直しは**累積のズレ**(3bps)で判定。送信直前に同じプールが2回
  赤字になったら、そのV2の手数料の前提(30bps)を捨てて45bpsに戻し、実測し直す
- 送信は「`simulateRoute`(eth_call)で正確な利益を確認 → `executeRoute`」。
  赤字と分かった経路は**段ごとに正確に見積もり直して犯人を名指し**し、
  プールが動くまで再判定しない
- 送信は**プール単位の錠**で並行(`NonceManager` が nonce を手元で管理、
  同時最大3本)。同じプールを使う経路だけ直列
- ガス代の見積もりは実測の gasUsed と実効単価から学習(上限は費用に数えない)
- コントラクトは新旧2版がある。新版(ガス削減版)には経路を `(pool, tokenOut, flags, feeBps)` で
  渡し、`flags` に「V3か」「投入通貨が token0 か」「getAmountOut を持つか」を bot が地図から
  入れる。旧版には従来の形で渡す。どちらかは起動後に `FLAG_V3()` で判別して覚える
- 最低利益 `MIN_PROFIT_USD=0.005`。取引上限$2000
- 30秒後に「誰が取ったか」を記録簿に残す。ダッシュボードの時刻は日本時間

## 実績
- 2026年9月17日に初の自動売買成功。午前中に4件成功・失敗0件、確定利益合計+$0.118
- 4件ともPolygonの2段で「V2でずれた直後にV3(0.05%)で戻す」型、確認時の粗利+40〜50bps、検知から送信準備0.18〜0.52秒
- 他者に先を越されたのは0件。一方で約1.5時間黒字判定が0件の時間帯があり、課題は速度より機会の数

## 確定した実測値
- Aerodrome/Velodromeのvolatileプール手数料は約1%
- ガス代目安: avalanche≒$0.001 / polygon≒$0.012〜0.017(2段) / arbitrum≒$0.015
- 「投入$2で利益$24」のような価格差はハニーポット。本物は投入額の0.1〜0.5%程度
- Polygon V2だけの経路の黒字は税トークンか極小案件ばかりだった
- PolygonのDEX出来高(30日): Uniswap約51%、RamsesX約16%、Metric約14%、QuickSwap約12%
- AvalancheのDEX出来高(30日): Pharaoh約78%、Uniswap約6%、Blackhole約5%、LFJ約2%
- 2026年9月17日に自前で測った値(scripts/pool-survey.js):
  - Polygon: V3 Swapの31.5%が未監視のファクトリー。最大の1社(0x411b0fac…、
    Algebra形式)だけで22.4%を占め、監視中の21ペア全てにプールがある
  - ガス代の単価は上限(maxFeePerGas)ではなく baseFee+priority で見積もる。
    ethers の maxFeePerGas は baseFee×2+priority なので、そのまま使うと過大になる
  - 2段の実測ガス使用量は256,409〜272,246(想定の35万は約1.22倍の過大だった)
  - 送信は同じチェーンで1件ずつ。並行させると同じ nonce を取り合い、片方が
    "replacement fee too low" で失われる(粗利$0.4155の機会を実際に失った)

## Base / Optimism を止めた判断の訂正(2026年9月17日)

停止時の理由は「2社がMEV抽出の8〜9割を占める最激戦区で、Aerodrome/Velodromeの
手数料1%も壁になる」だった。自前で測り直した結果、この根拠は誤りだった。

- 手数料1%が壁になるのは Aerodrome/Velodrome の volatile プールだけで、
  チェーン全体の話ではない。実測した活動量は両チェーンとも97%がV3で、
  V2は2〜3%しかない。1%のプールは主戦場ではなかった
- 「他者に先を越されて勝てない」も自前のデータでは裏付けられない。
  競合確認の実測は「自分4件 / 他者の裁定0件」
- 停止を決めたのは、ガス代を約1.8倍過大に見積もっていた時期。ガスが安い
  チェーンほど不当に不利に評価されていた

実測(2026年9月17日):

| | Base | Optimism |
|---|---|---|
| V2 / V3 の活動比 | 3% / 97% | 2.4% / 97.6% |
| 監視ペアで見つかったプール | 18件 | 27件 |
| 最小の手数料の壁 | 0.06%(0.01%+0.05%) | 0.06% |
| 2段のガス代 | $0.010 | $0.005(最安) |

Polygon の実績は壁0.35%なので、どちらも条件は良い。特に Optimism は
ガス代が最安で、同じペアに 0.01% / 0.05% / 0.3% / 1% が並んでいる。

再開には未監視ファクトリーへの対応(価格表を作れるようにすること)が前提。
環境変数を変えるだけでは、見つかったプールは経路に使われない。

[一度止めたチェーンは測り直せなかった]
getProviderForChain が ACTIVE_CHAINS のチェーンしか返さず、止めたチェーンは
RPCで読むことすらできなかった。そのため判断を見直す手段が無かった。
2026年9月17日に、読み取りの入口だけ getAnyChainConfig に変えて測れるようにした。

## フォークのプールを見積もれない問題(2026年9月17日)

Uniswap公式の QuoterV2 は quoteExactInputSingle(tokenIn, tokenOut, fee) の形で、
引数にプールのアドレスが無い。中に固定されたファクトリーからアドレスを計算
するため、フォークのプールには届かない。
一方 legIsUsable() はV3の段に価格表を要求するので、価格表が作れないプールは
経路に使えない。これが次の全てに共通する根本原因だった。

- Polygon の RamsesX(V3 Swapの22.4%)
- Optimism の未監視ファクトリー(27プール)
- Base の未監視ファクトリー(18プール)

対策は「プールのアドレスを直接受け取る見積もり」。コントラクトに quoteV3 を
足し、QuoterV2 と同じくわざと失敗させて受取量を返す。コールバックは実行と
同じものを使うので、対応済みの形式(uniswapV3 / pancakeV3 / algebra)は
そのまま扱える。bot側は「公式で求まらなかった分だけ」自前に回すので、
既存の Uniswap プールの動きは変わらない。

有効化は環境変数 ENABLE_FORK_QUOTER にチェーン名をカンマ区切りで指定する。
未設定なら自前の見積もりは一切走らない。コントラクトに quoteV3 を入れて
再デプロイするまでは有効にしないこと。

### 公式Quoterへの振り分けは「フォークかどうか」で決める

当初この振り分けを「手数料帯を持つか」で書いていたが、これは危険な誤り
だった。フォークにも手数料帯を持つもの(Uniswap形式のフォーク)があり、
それを公式Quoterに投げると、公式側に同じペア・同じ手数料帯のプールが
ある場合に **別のプールの価格が返ってくる**。それをフォークのプールの
価格表として保存すると、存在しない価格差=幻の利益機会を作り出す。
今は isForkFactory() でファクトリー単位に判定している。一覧に無い
ファクトリーは公式と断定できないため、安全側(フォーク扱い)に倒す。

### ファクトリーの style は実測済み。確認すべきは書き写し

一度「style は未検証」と書いたが、これは誤りだった。調査スクリプトは同じ
ファクトリーに getPool と poolByPair の両方を allowFailure 付きで投げ、
実際に住所を返した方を採っている(pool-survey.js の probeFactoryForPairs)。
Base 18件 / Optimism 27件というプール数も、その反応から得た実データ。

残る不確かさは「調査結果を V3_FACTORIES へ書き写す作業」だけで、実際に
チェックサムの大文字小文字を3件書き間違えていた。そのため
discoverV3PoolsForChain は fork のファクトリーごとに発見件数を
`[発見] polygon algebra-a(algebra): V3プール○件` の形でログに出す。
**ENABLE_FORK_QUOTER を有効にした直後、この行が0件でないことを必ず確認する。**

なお作業用コンテナからは外部RPCへ出られない(ネットワークポリシーで遮断)ため、
アドレスの実在確認はこのログか、Railway上での実行でしか行えない。

## Avalanche の調査結果(2026年9月17日)

3,000ブロック(約100分)を実測した。V2 797件 / V3 2,176件。

- 監視中(Uniswap 0x740b1c1d…)は **387回、全体の17.8%** しかない。
  **82%が未監視**で、Polygonの31.5%より深刻だった
- 未監視の上位3社のうち、監視中ペアにプールを持つのは1社だけ
  - `0x1128F23D…` Swap **924回**(42%)/ 監視中10ペアに **9プール**。
    うち流動性があるのは USDC/USDT 0.01%、USDC/WAVAX 0.01%、
    USDC/WAVAX 0.05% の3件。照会形式は getPool(Uniswap V3形式)
  - `0x512EB749…` Swap 473回 / 監視中ペアにプール **なし**
  - `0xAe6E5C62…` Swap 339回 / 監視中ペアにプール **なし**
- `0x3e603C14…` は Swap 8回と静かだが、監視中ペアに **13プール**あり
  流動性が厚い(DAI.e/WAVAX 0.3%、WAVAX/WETH.e 0.3% など)。
  動きが遅いプールは価格が取り残されやすく、相手側として価値がある
- したがって Avalanche で取れる上積みは、いま見ているペアのまま
  **V3の活動量が約2.4倍(387→1,311回)** になる分

## Algebra形式のプールへの対応(2026年9月18日に実施)

下の節で「まだ使えない」と書いた2点を直し、`ALGEBRA_SUPPORTED = true` にした。

1. **状態の読み取り** — `multicall-reserves.js` が `globalState()` に対応。
   返り値の並びは版によって違う(Algebra V1は7個、Integralは6個で中身も別物)
   ため、**ABIで丸ごと復号せず、共通する先頭2語(uint160 price / int24 tick)
   だけを自前で読む**。丸ごと復号すると版が違うだけで価格を取れなくなる。
   int24 は32バイトに符号拡張されて入るので、負のtickを戻す処理を入れてある。
   一度 globalState で読めたプールは覚えておき、次から slot0 を試さない
2. **WebSocketの購読** — 手数料を含む Algebra の Swap 識別子2種を追加した。
   `Swap(…,int24,uint24)` と `Swap(…,int24,uint24,uint24)`。
   先頭4つ(amount0/amount1/price/liquidity)の位置はどれも同じなので、
   復号は `decodeV3SwapData` をそのまま使える
3. **見積もり** — 自前の `quoteV3`。コントラクトは `algebraSwapCallback` を
   元から持っていたので、コントラクト側の変更は不要だった

このとき `dex-onchain-realtime.js` には **import が1つも無かった**。
ethers を使うには import の追加が要る(無いと起動時に ReferenceError で落ちる)。
あわせて、手書きしてあった識別子4件を `ethers.id()` の計算に変えた
(検算したところ4件とも正しかったが、手書きは過去の事故の原因そのものなので)。

## (上の対応前の記録)Algebra形式のプールは、まだ使えない(2026年9月17日に判明)

PR #9 で algebra-a(Polygon、V3 Swapの22.4%)と algebra-b(Base)を
V3_FACTORIES に足したが、**今のコードではこの2社のプールは必ず脱落する**。
発見はできても、次の2つが Uniswap形式しか想定していないため。

1. **状態の読み取り**: `fetchV3StatesBatch` は `slot0()` しか呼ばない。
   Algebra は `globalState()` で、返り値の並びも版によって違う
   (先頭の uint160 price と int24 tick は共通)
2. **WebSocketの購読**: `dex-onchain-realtime.js` の ALL_TOPICS は
   Uniswap V3 の Swap 識別子 `0xc42079f9…` だけ。手数料を含む
   Algebra の Swap `0x121cb44e…` は受信できない

Avalanche の調査で `0x5F1dddbf…`(Algebra形式)の流動性が
「読めず」と出たのが、1つ目の実例。

**この2点を直すまで、algebra を含むチェーンで ENABLE_FORK_QUOTER を
有効にしても、そのプールは監視枠とRPCを使うだけで一度も使われない。**

そのため v3-pools.js に `ALGEBRA_SUPPORTED = false` を置き、Algebra形式の
ファクトリーは探索対象から外している。上の2点を直したら true にする。
それまでは Uniswap形式のフォーク(univ3-fork-a〜f)だけで効果を測る。

## フォーク見積もりの効果(2026年9月18日、Polygonで実測)

`ENABLE_FORK_QUOTER=polygon` を有効にした前後の実測値。

| 項目 | 前 | 後 |
|---|---|---|
| Polygon V3プール | 80 | 166 |
| 監視プール合計 | 416 | 502 |
| 判定に使えるV3プール | 72/84 | 130/170 |
| 価格表 | 288/344 | **401/516** |
| RPC 毎分 / 月末見込 | 72 / 7% | **72 / 7%(変化なし)** |

発見のログは `univ3-fork-a 59件` / `univ3-fork-b 27件` で、書き写しに誤りが
無かったことも確認できた。

公式Quoterで作れる表の数は変わっていないので、**増えた113件はすべて自前の
quoteV3 が作ったもの**。一度も見積もれなかったプールが判定に入った。
RPCが増えていないのは、Multicall3で束ねているため。

### コントラクト確認で見たこと(再デプロイ前の手順)

`quoteV3` は通常の取引と同じコールバックを使い、中間の足も見積もりも
どちらも `data.length == 0` で入ってくる。区別は `quoting` フラグだけなので、
これが実取引に漏れると支払いの代わりに QuoteResult で巻き戻り、全取引が
失敗する。安全である根拠は次の3つ。

1. `quoting = true` を書くのは `quoteV3` だけで、`quoteV3` は必ず revert して
   終わる。書き込みは毎回巻き戻り、成功する取引の開始時点では必ず false
2. `quoteV3` には `require(!inFlashSwap)` があり、実行中には入れない
3. 実取引の1段目は data が空でないため、この分岐に来ない

## 「あと何bpsで黒字だったか」を測る(2026年9月18日に追加)

Polygonのカバー率を埋めたあとも、20分で18万回の経路計算に対して粗利が
プラスの経路は0件だった。つまり問題は「見えていないプールがある」ことから
「価格のズレが手数料の壁+ガス代を超えない」ことへ移っている。

利益が出る条件は `価格のズレ > 手数料の壁 + ガス代/投入額`。実測値を入れると:

| チェーン | 壁(最小) | ガス代 | $5投入時のハードル | $50投入時 |
|---|---|---|---|---|
| Polygon | 0.35% | $0.012 | 0.59% | 0.37% |
| Optimism | 0.06% | $0.005 | 0.16% | 0.07% |
| Avalanche | 未実測 | $0.001 | — | — |

HANDOVERに記録した実勢は「本物の価格差は投入額の0.1〜0.5%程度」なので、
Polygonのハードル0.59%は分布のほぼ上端にある。これが0件の理由。

そこで finalize が捨てる前に「一番良かった時の利回り(bps)」を記録し、
段階ごとに数える仕組みを入れた(opportunity-scanner.js の nearMiss)。
**RPCは一切増えない。findBestAmount がすでに計算した値を数えるだけ。**

生存ログの `[惜しい]` 行と、ダッシュボードの「あと何bpsで黒字だったか」で
見られる。`countIfWallDrops(chain, bps)` は「壁がbps下がったら何本が
粗利プラスへ変わるか」を返す(段をまたぐ分は数えないので少なめに出る)。

既定の比較幅は29bps(= Polygonの35bps − Optimismの6bps)。環境変数
WALL_DROP_BPS で変えられる。**Optimismへ移す判断は、この数字を見てから行う。**

### 最初の版の欠陥と、その修正(同日)

最初の版は**評価するたびに1件数えていた**。Polygonは21ペアしか無いのに
43分で50万回評価するため、同じ経路が何千回も数え直され、「+66件」が
66個の機会なのか1個を66回見たのかを区別できなかった。Avalancheで
「-20〜-10bpsに200件、隣の段は0件」という不自然な穴が出たのがその証拠。

今は**経路ごとに「今までで一番良かった利回り」だけを持つ**(routeBest)。
件数は「届きうる別々の経路の本数」になる。キーはチェーン+始点トークン+
プール列で、上限50,000件(NEAR_MISS_ROUTE_LIMIT)。

あわせて**壁の高さでも分けた**。手数料1%のプールを2段通れば壁は200bpsで、
価格がどれだけ動いても黒字にならない。こうした構造的に無理な経路が
Polygonでは99.4%を占め、届きうる経路の信号を埋もれさせていた。
既定では壁60bps以下(REACHABLE_WALL_BPS、0.3%+0.3%まで)に絞った分布を出す。

### 43分間の実測(修正前の版・回数ベース)

| チェーン | 評価した経路 | 粗利プラス | 壁が29bps下がれば |
|---|---|---|---|
| polygon | 504,369 | 17 | +66(4.9倍) |
| arbitrum | 135,752 | 152 | +1,206(8.9倍) |
| avalanche | 33,684 | 0 | +200 |

生存ログの「粗利プラス169」と分布の先頭段の合計(17+152+0=169)が一致し、
計測そのものは正しいことを確認した。

**ただし Arbitrum の数字は信用してはいけない。** ARBITRUM_WSS_URL が未設定で
受信0件、定期読み直しの古い価格で判定しているため、実在しない価格差が出る。
152本が粗利プラスと出ながら取引が1件も成立していないのがその裏付け。
**ARBITRUM_WSS_URL を設定するのが、いちばん安く情報量の大きい次の一手。**

### 投入額の上限を上げた(2026年9月18日)

上の「取引上限$2000は到達できない」への対応。ただし**価格表を伸ばすだけでは
逆効果**で、先に投入額の探し方を直す必要があった。

findBestAmount は投入額を**上限に対する比率**で探しており、最小が0.01だった。
上限$300なら$3から探せるが、価格表を$2000まで伸ばすと最小が$20になる。
**実測の最適額は$1.78〜$8.05で全て$20未満**のため、伸ばすと今できている
取引を取り逃す。検証では上限$2000のとき理想の60%の利益しか取れなかった。

そこで小さい側を対数的に細かく刻む比率に変えた(最小0.0005)。同じ検証で
上限$300/$1,000/$2,000のいずれでも理想の100%を取れることを確認した。
そのうえで QUOTE_SAMPLES_USD に 1000 と 2000 を足した。倍率は従来と同じ
約3倍を保っている(間隔を空けると点と点の間の補間が過大になるため)。

**効果があったかは hitCap で測る。** 生存ログの `粗利プラスN(上限張付M)` の
M が、投入額が上限の95%以上に張り付いた回数。**0のままなら上限は効いて
おらず、刻みを増やした分のRPCが無駄なので QUOTE_SAMPLES_USD を元に戻す。**

RPCの増加見積もり: 価格表の子呼び出しが 864/分 → 1,152/分、束ねた後で
約29/分 → 約38/分。全体では毎分84 → 約94、月末見込 8% → 約9%。

### (対応前の記録)取引上限$2000は到達できない

V3を含む経路の投入額は、価格表の最大点(QUOTE_SAMPLES_USD の $300)で
頭打ちになる(routeMaxAmountIn)。ダッシュボードの取引上限$2000は
V3を含む経路には効いていない。刻みを伸ばせば、同じ価格差でも利益が増え、
ガス代の比率が下がってハードル自体も下がる(Polygonで$5→$50なら
0.59%→0.37%)。findBestAmount が最適な額を探すので、上限を上げても
無理に大きくは張らない。

## 「壁が下がっていたら、いくら取れたか」の試算(2026年9月18日)

本数だけでは戦略の上限が分からないため、**金額**まで出せるようにした。
生存ログの `[試算]` 行と、ダッシュボードの表で見られる。

各サンプル額について「手数料が W bps 低ければ受取がその分増える」として
利益を計算し、経路ごとに一番良かった値を保持する。**simulateRoute の
呼び出しは増えないのでRPCは増えない。** 既定の W は 29 / 50 / 100
(環境変数 WHATIF_WALL_DROPS)。

100bps は現実には到達できない(Polygonの壁35bps、Optimism 6bps、差は29bps)。
**「壁を極限まで下げても $いくらにしかならない」という天井を測るための値。**

### この数字は必ず多めに出る

- 経路ごとの**最良の瞬間**だけを足している(同時に全部は取れない)
- 複数の経路が同じプールを共有していても別々に数えている。
  実際には1つの価格差は1回しか取れない
- 自分が取れば価格が動くが、その分を引けていない
- 競争を考慮していない。壁が低ければ他社も参入する

**期待できる金額ではなく、この戦略の天井として読むこと。**
天井が小さければ、それ以上の投資(チェーン追加・速度改善)は見合わない。

## 動いていた構成と、動かなくなった構成(2026年9月18日)

**成功した7件は全て9月17日以前**で、新コントラクトを入れた 00:24 UTC 以降は
取引が1件も成立していない。過去ログの `[判定の精度]` を全デプロイ分遡ると、
**送信直前まで進んだ6件すべてがチェーン上では赤字(精度 0/6 = 0%)**だった。

| | 成功した7件 | 失敗した6件 |
|---|---|---|
| 時期 | 9/17以前 | すべて9/18 |
| チェーン | Polygon のみ | Polygon 2 / Arbitrum 4 |
| 投入額 | $1.78〜$8.05 | $8.97〜$276 |
| 経路 | Uniswap + sync発見(V2) | algebra-a / arbitrum / 3段 |

失敗6件の実測誤差:

| 段数 | 誤差 |
|---|---|
| 2段(Arbitrum) | −3 〜 −14bps |
| **3段(Polygon)** | **−50 〜 −94bps** |

**誤差は経路の複雑さに比例して増える。** 狙う利幅は5〜50bpsなので、3段経路の
誤差(50〜94bps)は利幅を上回っている。**それは機会ではなくノイズ。**

### 考え方の誤り

動いていたものは「小さくて単純」だった(少数の実績あるプール、2段中心、
小さな投入額、PolygonのUniswap)。9月18日にプールを3倍にし、Algebraと
フォークを足し、新チェーンの受信を繋ぎ、3段経路を広げ、投入額を30倍にした。
**部品を足すたびに見積もりの誤差が積み上がり、動いていたものまで埋もれた。**

「機会が足りない」と考えて範囲を広げ続けたが、**広げるほど精度が落ちる**
という関係を見ていなかった。

### 進め方は逆向きにする

動いていた構成まで戻し、そこで精度を測り、**1つずつ足して毎回精度を測る**。
落ちたらその部品が原因。

### 見積もり誤差の測り方を直した

`verifyV3Calculations` は**$20でしか測っていなかった**。価格表の点は
$1,3,10,30,100,300,1000,2000 で、$20は「$10〜$30」という狭い区間にあり
補間の誤差が最も小さい場所。一方、実際に赤字と確定した取引は $102 と $276、
つまり「$100〜$300」という広い区間にある。**いちばん簡単な点だけを
測っていた。**

今は VERIFY_AMOUNTS_USD(既定 20,200,700)の各額で測り、**%ではなくbps**で
集計する(狙う利幅が5〜50bpsのため、1%=100bpsの閾値では粗すぎた)。
生存ログの `[見積もり誤差]` に投入額ごとの平均・最悪・過大率が出る。

**判断の基準**: 誤差が数bps以内なら見積もりは使える(原因は個別の不具合)。
10bps以上なら**見積もりの精度が戦略の前提を満たしていない**ので、プールも
チェーンも投入額も無意味。見積もりの作り方そのものを変える必要がある。

## 価格表の作り直し条件が壊れていた(2026年9月18日に修正)

作り直しの判定に `lastMovePct`(**今回の更新1回ぶんの変化率**)を使っていた。
これは価格表を作ってからの累積のズレではない。

**0.09%ずつ100回動いても一度も閾値を超えず、ズレは無制限に積み上がる。**
再現した結果、sqrtPriceが0.01%ずつ50回動くプールでは:

| | 作り直し | 最終的なズレ |
|---|---|---|
| 旧(1回ぶん) | **0回** | **100bps** |
| 新(累積) | 25回 | 3bps以内 |

100bpsのズレは狙う利幅(5〜50bps)を大きく上回る。送信直前に赤字と確定した
4件の誤差(−3.2 / −5.2 / −8.9 / −13.6bps)が、旧閾値0.1%=10bpsの周辺に
収まっていたことと整合する。**これが幻の利益の正体。**

### 直したこと

- `updateV3FromSwap` が `quoteDriftPct`(表を作った時点からの累積のズレ)を
  計算する。基準は `markQuoteBase` が価格表の作成時に記録する
- 作り直しの判定3箇所すべてを `quoteDriftPct` に切り替えた
- `QUOTE_REBUILD_MOVE_PCT` の既定を 0.1%(10bps)→ 0.03%(3bps)。
  **意味が「1回ぶん」から「累積」に変わっている**点に注意
- 作り直しを**ズレの大きい順**にした。枠が限られているので、
  いちばん誤差が出ているプールから使う

### RPCは増えない

作り直しの本数は `QUOTE_TABLE_PER_TICK`(既定6プール/5秒 = 72プール/分)で
上限が決まっており、閾値を下げても変わらない。変わるのは「限られた枠を
どのプールに使うか」だけ。

### 追いついているかの見方

生存ログの `[価格表の鮮度]` に「基準あり/ズレ超過/最大ズレ」が出る。
**ズレ超過が減らない場合、閾値ではなく作り直しの処理能力が上限**なので、
QUOTE_TABLE_PER_TICK を上げる(RPCは増える)。
V3プール279件を一巡するのに約3.9分かかる計算。

## フォークのプールは環境変数では消えない(2026年9月18日に判明)

`ENABLE_FORK_QUOTER` を空にしても、Polygonの V3 は 191件のままだった。
プール地図は `/data` に保存され起動時に復元されるため、`activeV3Factories`
(発見=追加の制御)では既存のプールを消せない。

取引には使われない(価格表が作れないため)が、**購読枠とRPCを消費し続ける**。
除去には、起動時に `activeV3Factories` に無いファクトリーのプールを
地図から外す処理が要る。**未実施。**

## 【設計】見積もりを「作り置き」から「候補が出てから」へ変える(2026年9月18日)

**これは設計であって、まだ実装していない。** 対象を広げる前に必ずこれを済ませる。

### なぜ今のやり方だと広げられないのか

今は全てのV3プールについて、両方向×8点の価格表を常時作り置きしている。
作り直せるのは `QUOTE_TABLE_PER_TICK`(6プール/5秒 = 72プール/分)までで、
**費用も鮮度もプール数に比例する**。

| V3プール数 | 一巡にかかる時間 |
|---|---|
| 279(現在) | 3.9分 |
| 1,000 | 14分 |
| 5,000 | 69分 |

**69分前の価格で判定すれば、出てくる機会は全部が幻になる。**
プールを3倍にした9月18日に送信直前の6件が6件とも赤字になったのは、
この関係がすでに効き始めていたということ。
つまり**今のやり方のままでは、広げるほど精度が落ちる**。閾値をどう変えても
枠の上限は動かないので、やり方そのものを変えるしかない。

### 変える内容

**「現在価格でふるいにかけて、通ったものだけ正確に見積もる」。**

1段の受取比率は、どんなAMMでも**投入額が増えるほど悪くなる**
(V2の x·y=k も、V3の集中流動性も、出力は投入に対して上に凸)。
したがって**投入額ゼロの極限での比率(現在価格)が、その段で得られる
いちばん良い比率**になる。

経路全体でも同じで、

```
∏(現在価格ᵢ × (1 − 手数料ᵢ)) ≤ 1  ならば、どんな投入額でも黒字にならない
```

これは近似ではなく**必ず成り立つ**。つまり現在価格だけを見たふるいは、
**本物の機会を取りこぼさない**(見逃しゼロ)。通り過ぎるものは出るが、
それは次の段で正確に見積もるので問題にならない。

### この方式の何が良いのか

- **現在価格は作り置きが要らない。** sqrtPriceX96 も準備量も、Swap/Syncを
  受信するたびに更新される。**常に最新で、RPCはゼロ、補間の誤差もゼロ**
- **費用が「プール数」ではなく「候補の数」に比例する。**
  プールが何万件あっても、ふるいを通る本数しか見積もらない
- **今回の幻の正体(累積のズレ)が原理的に消える。** 表を持たないので
  古くならない
- **`V3必須` と `両トークンがV3プールに` の制限を外せる。** V2の段は元から
  正確に計算できており、V3の段もふるいには表が要らなくなるため

### 手順(この順に進める)

1. **まず測る。実装を変える前に、ふるいを通る本数を数えるだけの計測を入れる。**
   RPCはゼロ。通過率が分からないまま切り替えると、候補が毎分数千件出た場合に
   RPCが破裂する。**この測定を飛ばさないこと**
2. 経路の現在価格の積 `cycleEdgeBps(legs)` を計算する(メモリだけ)
3. ふるいを通った経路について、その経路の段だけの価格表を**その場で**作る
   (2段×8点=16件をMulticall3で1回にまとめられる)。作ったら捨てる
4. `legIsUsable` のV3条件を「価格表がある」から「現在価格と流動性がある」へ
5. 作り置きの `refreshQuoteTables` を止める(または上位数十プールの
   温存用にだけ残す)
6. `[判定の精度]` が回復していることを確認してから、制限①②③を外す

### 残る注意点

- ふるいは「**どれかの投入額で黒字になりうるか**」しか答えない。
  ガス代は投入額に依存するので、ガスのハードルは今までどおり
  `findBestAmount` の側で見る
- 通過率が高すぎる場合は、ふるいの閾値を 0bps ではなく
  「ガス代が見合う最小の利幅」まで引き上げて絞る

## 始点トークンの逆算を広げた(2026年9月18日)

### それまで何が起きていたか

始点に使えるかは「桁数と価格が分かるか」だけで決まる。その価格の逆算が
`derivePriceFromStablePools` で、**安定通貨と直結したV2プールしか見ていなかった**。
そのため次の2つが丸ごと価格不明になり、始点から外れていた。

1. **V3にしかないトークン** — Base/Optimismでは活動の97%がV3
2. **安定通貨と直接のプールが無いトークン** — WETH経由などは辿れない

しかも2周まわす作りになっていたが、起点の条件が静的な `stable: true` の
一覧だったため、**2周目で新しく増えるものは1件も無かった**(実質1周)。

### 直した内容(index.js のみ。RPCは1件も増えない)

- **相手が安定通貨でなくてもよい。** その周で価格が確定した通貨なら起点にする
- **V3プールも価格の起点にする。** 現在の価格帯の仮想準備量 (L/√P, L·√P) を
  使う。この2つの比は現在価格そのものになる(USDC/WETHの既知の値で
  両方向とも一致することを確認済み)
- **`PRICE_HOPS`(既定4)回まで隣へ辿る。** 安定通貨 → WETH → その隣、と広がる。
  新しく価格が付かなくなった時点で打ち切る
- **毎回ゼロから作り直す。** 以前は「まだ価格が無いものだけ」を埋めていたため、
  一度付いた価格は二度と更新されなかった。価格は投入額の計算と利益のUSD換算の
  両方に使うので、古いままだとガス代とのハードル比較がその分ずれる

### 安全側の歯止め

価格を間違えると投入額の計算ごと狂うため、**起点にするプールには
`MIN_PRICE_SOURCE_USD`(既定$5,000)の深さの下限を掛けている**。
同じトークンに複数の経路がある場合は、いちばん深いプールを採る。
V3の仮想準備量は価格帯が狭いと深さを多めに見積もるが、その分はこの下限で落とす。

### 効果の見方

生存ログの `始点N` とダッシュボードの「始点に使える通貨」。
**増えた分だけ `[判定の精度]` が落ちていないかを必ず併せて見る。**
落ちた場合は `PRICE_HOPS=1` に下げるか `MIN_PRICE_SOURCE_USD` を上げる。

## ふるいの通過率の計測(2026年9月18日に追加。設計の手順1)

「作り置きをやめる」設計の**手順1**。切り替えると、ふるいを通った回数が
そのまま見積もりの回数(=RPC)になる。**通過率が分からないまま切り替えると
RPCが破裂する**ので、先に数えるだけの計測を入れた。

生存ログの `[ふるい]` 行で見る。

```
[ふるい] 評価12,345 通過[0bps超:230(毎分18) 5bps超:41(毎分3) 10bps超:12(毎分1) 30bps超:0(毎分0)]
         別々の経路87 表ありでの通過64 最良41.2bps
```

- **毎分の通過回数が、切り替え後に必要なRPCの見積もりそのもの。**
  段ごとに出しているので、閾値をどこに置けばRPCがいくつになるかが分かる
- **「表ありでの通過」との差**が、いま価格表が無いせいで**見えていない候補**
- `scanTwoStep` の先頭で、**価格表の有無に関係なく**数える。
  今の判定は価格表のある段しか使えないため、その条件を外さないと
  「切り替えたら候補がいくつ出るか」を測れない

**RPCは1件も増えず、判定の動作にも一切影響しない。**

### ふるいが正しいことの検算(V2の厳密式と総当たりで比較)

| 状況 | ふるい | 総当たりの最良 | 判定 |
|---|---|---|---|
| 価格差なし | −59.91bps | −59.92bps | 通過なし |
| 1%安い側あり | **+39.49bps** | **+39.48bps** | 通過あり |
| 0.6% | −0.27bps | −0.28bps | 通過なし |

いずれも**ふるいの値が総当たりの最良以上**(=上限になっている)で、
**本物の機会を落とした例は無かった**。差が0.01bps程度しかないのは、
現在価格が「投入額ゼロの極限」であり、小さい投入額ではそれにほぼ等しくなるため。

### 最初の計測は計器が壊れていた(2026年9月18日、本番で判明)

入れた直後の実測はこうだった。

```
[ふるい] 評価1,807,536 通過[0bps超:457,738(毎分49,262) …]
         別々の経路5,420 表ありでの通過364,538 最良10,084,520,773,245.5bps
```

**この数字は信用してはいけない。**欠陥が2つあった。

**① 壊れたプールを数えていた。** 最良が **10垓%**。準備量が1weiしかない
プールや死んだトークンの現在価格はいくらでも極端になるので、ふるいは
必ず通る。HANDOVER の「投入$2で利益$24 はハニーポット」と同じ話。
通過の80%が30bps超という、実態としてあり得ない分布になっていた。
→ `SPOT_SCREEN_SANE_MAX_BPS`(既定1000 = 10%)を超える利幅は
**異常として別枠で数え、通過から外す**。

**② 「毎分の通過回数」がRPCの見積もりになっていなかった。**
通過は**評価のたび**に数えていたが、別々の経路は5,420本しかない。
同じ経路を同じ状態のまま何百回も数え直していた。
**同じ状態の同じ経路なら、見積もりは1回で足りる。**
→ 経路ごとに状態の署名を持ち、**変わってから初めて通った回数**
(`fresh`)を数えるようにした。生存ログの「要見積もり」がそれ。

あわせて、**別々の経路を利幅で分けた内訳**も出すようにした。
回数ではなく本数なので実態に近い。

**これは9月18日に自分で挙げた失敗の型そのもの**(「計器の出力を、計器を
検証する前に事実として報告した」)。**新しい計器の最初の数字は、必ず
おかしな値が無いか見てから使うこと。** 今回は `最良` を一緒に出していた
から気づけた。

### ふるいに「金額」を足した(2026年9月18日)

計器を直したあとの実測。

```
[ふるい] 評価794,404 要見積もり[0bps超:毎分1,140 30bps超:毎分716]
         経路3,908本[0〜5bps:158 5〜10bps:126 10〜30bps:460 30〜100bps:882 100bps超:2,282]
```

**そのままでは切り替えられない。**

| 閾値 | 毎分の見積もり | 月間RPC | 枠2,000万に対して |
|---|---|---|---|
| 0bps超 | 1,140回 | 4,790万 | **240%** |
| 30bps超 | 716回 | 3,490万 | **175%** |

そして**経路の81%が30bps超**。HANDOVERの実測では本物は10〜50bpsなので、
この分布は本物ではあり得ない。

**原因は、ふるいが割合(%)しか見ていなかったこと。**
流動性$50のプールで500%の価格差があっても、取れるのは数ドルで、
その大半は税トークンか死んだプール。**深さは無料で手に入る**
(V2は準備量、V3は仮想準備量)ので、金額まで見るようにした。

```
今まで: 価格差 > 0
これから: 価格差から取れる金額 − ガス代 > $0.01
```

**RPCは1件も使わない。** 浮動小数で x·y=k を8点だけ試す。

### 割合で切ろうとして失敗しかけた(同日、計器の検証中)

最初は「1000bps(10%)超は異常」として落としていた。検証すると、
**薄いプールの大きな価格差が全部この上限で落ちていた**。落ちる結果は
正しかったが、落としていたのは**上限であって金額の条件ではなかった**。

割合で切ると「深いプールの大きな価格差」という本物まで落とす。
本物かどうかを決めるのは金額なので、上限は10000bps(100%)まで上げ、
**「10垓%」のような計算が壊れている値だけを弾く**役に戻した。

### 検証した内容

| 確かめたこと | 結果 |
|---|---|
| 浮動小数の見積もり vs 厳密な整数計算 | **$1.8554 / $1.8554(誤差0.0000%)** |
| 深い($100万)、価格差1% | 通過、見積利益 **$1.8554** |
| **薄い($100)、価格差は同じ1%** | **落ちる** |
| 異常として弾いた回数 | 0(壊れた値だけ) |

**同じ価格差でも、深さで正しく分かれる。**

あわせて `[ふるいの実物]` 行で、いちばん良かった経路を実際のDEX名・
アドレス・価格差・壁・見積利益つきで出すようにした。
**2,282本が何なのかを推測で語らないため。**

### 金額の条件の実測と、作り置きをやめる仕組み(2026年9月18日)

```
[ふるい] 評価857,072 価格差だけ[0bps超:毎分864 30bps超:毎分478] 金額まで通過:毎分0
         経路4,592本 利益[赤字:4,492 〜$0.01:78 $0.01〜0.1:15 $0.1〜1:2 $1超:4]
```

3回の計測で **毎分 3 / 22 / 0 件**。

| | 毎分 | 月間RPC | 枠2,000万に対して |
|---|---|---|---|
| 価格差だけ | 1,140〜3,358 | 4,790万〜1億4千万 | **240〜700%** |
| **金額まで** | **0〜22** | **最大95万** | **4.8%** |

**約50〜100倍落ちた。切り替えられる。**
しかも切り替えれば作り置き(約38回/分)を止められるので、実質ほぼ増えない。

**別々の経路4,592本のうち、$0.01を超えたことがあるのは21本(0.46%)だけ。**
残りの97.8%は、割合では光って見えても金額にすると赤字だった。
実績($0.008〜$0.37)とも整合する。

### 実物を見て分かったこと

```
[ふるいの実物] polygon sync発見:0x7e2eaa…→uniswap-v3:0x9174d3…
               始点0x2791bca1…(USDC.e) 価格差6593.1bps 壁60bps 見積利益$10.5872
```

**65.9%の価格差が4分間まったく動かない**(値が1回も変化しない)。USDC.e で
65.9%の裁定が4分放置されることはあり得ない。誰も取らないのは**取れないから**で、
税トークンかハニーポットの可能性が高い。**ただし断定するのは2段目の仕事**で、
`simulateRoute` を1回投げれば白黒つく。それがふるい方式の設計そのもの。

なお同じ経路が2本出ているのは誤りではない。1つの循環を別々の始点から
数えているため(A→B→A と B→A→B は同じ積になる)。

### 仕組み: 要求されたものから作る

- ふるいが**金額の条件まで通した経路**について、価格表が無くて判定できない
  V3の段があれば `quoteDemand` に積む(`takeQuoteDemand()` で取り出す)
- `refreshQuoteTables` はこれを**最優先**で作る。次にズレの大きい順、
  最後に残り枠で作り置き
- **`QUOTE_TABLE_FILL_ALL=false` で作り置きを止められる。** これで費用が
  「プール数」ではなく「候補の数」に比例するようになり、**プール数の上限が無くなる**
- 既定は `true` のまま。生存ログの `要求で作成N/M` が動いているのを
  確認してから切り替える

### 確認したこと

| 確かめたこと | 結果 |
|---|---|
| 64.7bps・見積利益$9.82の経路 | 金額の条件を通過 |
| その経路のV3プール | **作成を要求した** |
| 同じ経路のV2プール | **要求されない**(表が要らないため) |
| 取り出したあとの待ち行列 | 空になる |

### 次にやること

`[ふるい]` の毎分の通過回数を数時間ぶん見てから、

1. 通過が毎分数十件までなら、**そのまま切り替えられる**
   (候補ごとに2段×8点=16件を1回のMulticallで見積もる)
2. 毎分数百件を超えるなら、閾値を「ガス代が見合う最小の利幅」まで上げて絞る

## 始点が18件から動かなかった本当の原因(2026年9月18日)

価格の逆算を広げても、本番の実測は `始点18` のままだった。**原因は逆算では
なく、その1つ上流の「V3プールの探し方」にあった。**

```js
// index.js  discoverV3PoolsForChain(修正前)
const tokens = Object.keys(getKnownTokens(chain));   // 手書きの4〜7種だけ
```

V2は44,000件のプールを発見しているのに、**V3の探索だけが手書きの一覧に
縛られていた**。さらに絞り込みが「両トークンがV3プールにあるV2だけ残す」
ため、地図に入るトークンがこの一覧に固定される。

実測がそれを裏付けていた。

```
[準備完了] polygon: V2 360件 / V3 187件、桁数0トークン
                                    ↑ 未知のトークンが1件も無い
polygon 7 + arbitrum 6 + avalanche 5 = 18  ← 始点18と完全一致
```

### 直した内容

1. **探索対象を「V2で実際に流動性のあるトークン」まで広げた**(推測しない)。
   深さは**相手側**の価値で測る。相手が手書きの一覧にある通貨なら、
   こちら側の桁数も価格も知らないまま深さが求まる。
   `V3_DISCOVERY_TOKENS`(既定24種)、`MIN_DISCOVERY_DEPTH_USD`(既定$50,000)
2. **ファクトリーへの照会を束ねた**(`findV3PoolsBatch`)。
   `findV3Pool` は1ペア1回のRPCで、24種だと 276ペア×13通り=**3,588回**に
   なる。Multicall3で束ねて**十数回**に収めた。これが無いと広げられない
3. **探索を絞り込みより先に動かした。** 順番が結果を決める。先に絞り込むと、
   これから対象にするトークンのV2プールが判断材料ごと捨てられる
4. **中身が空のV3プールは地図から外す。** 広げると「作られただけで流動性が
   無いプール」も見つかる。残すと購読枠と受信を食うだけ。
   ただし読み取りがまとめて失敗した時に地図を壊さないよう、
   **失敗が半分を超えたら何も外さない**

### 広げたのに0件だった(2026年9月18日、本番で実測)

探索対象を広げた直後の実測は **`[探索対象] polygon: 手書き7種のみ(深さ$50,000
以上の追加候補なし)`** で、**追加候補が0件**だった。

原因は深さの下限ではなく、**保存済みの地図に準備量が入っていなかった**こと。

```js
// loadPoolMap(修正前)
registerPool({ ...e, raw0: 0n, raw1: 0n, ... });   // 準備量を捨てていた
```

準備量を読むのは `prepareChain` で、これは絞り込みの**後**に走る。探索対象を
決める時点では全プールの準備量が0なので、深さがどれも0になり、
誰も下限を超えられなかった。

**直したこと**

1. **地図に準備量を残す**(`snapshotFullMap` / `savePoolMap` / `loadPoolMap`)。
   復元した準備量は古いので `updatedAt: 0` を付け、読み直しの対象にする
2. **探索対象を決める前に、必要な分だけ準備量を読む**(`loadDepthProbeReserves`)。
   対象は**片側が手書きの一覧にある通貨**のプールだけ。物差しが無いプールは
   深さを測れないので読んでも意味がない。上限は `MAX_DEPTH_PROBE_POOLS`(既定15,000)。
   次回以降は保存済みの値が使えるので、ここで読む件数は自然に減っていく

**教訓**: 「広げた」と「広がった」は別物。`[探索対象]` の行を出していたから
0件だと分かった。**効果を測る行を一緒に入れること。**

### 中身が空のV3プールの除外は効いた(同日、実測)

| チェーン | 除外したV3 | 監視プール |
|---|---|---|
| polygon | 36件 | 523 → 487 |
| arbitrum | 3件 | 114 → 111 |
| avalanche | 9件 | 28 → 19 |

価格表の待ち行列も 184 → 149 に減った。**一度も見積もれないプールが
購読枠と価格表の枠を占めていた**ということ。

### 効果と費用の見方

- **効果**: 生存ログの `始点N`、`[探索対象]` 行、`[準備完了]` の桁数トークン数
- **費用**: 探索そのものは起動時の十数回だけ。**増えるのは受信**
  (枠の内訳で `受` が大半を占める)。監視プールが増えた分だけ比例するので、
  `枠[…月末見込N%]` を必ず見る。上がりすぎたら `V3_DISCOVERY_TOKENS` を
  下げるか `MIN_DISCOVERY_DEPTH_USD` を上げる
- **精度**: `[判定の精度]` が落ちていないかを併せて見る。落ちたら広げすぎ

### 確認したこと(作業用コンテナから外部RPCへは出られないため、机上で検算)

- 深さ$200,000のトークンは選ばれ、$1,000のトークンは除かれる
- 選んだトークンのV3プールを登録すると、そのV2プールが絞り込みを**生き残る**
  (これが始点を増やす経路そのもの)
- `getPool` の識別子が Uniswap V3 の既知の値 `0x1698ee82` と一致。
  未作成(ゼロ住所)を正しく弾ける

## 作り置きをやめた結果(2026年9月18日 21:42 UTC、本番で実測)

`QUOTE_TABLE_FILL_ALL=false` にして作り置きを止めた直後、**9月17日以来はじめて
取引が成立した。**

```
[実行] 2step polygon sync発見→uniswap-v3(0.05%): 送信します
       (投入$4.00 粗利$0.0191/+47.7bps ガス$0.0125 確認111ms 準備470ms)
[実行] 送信: 0xa40cd9df1378eba61bcd25af5f5d8667f5c043888a7555aefb8375b43278bf40
[実行] 完了: ブロック94041817 ガス276271
[実行] 確定: 粗利+$0.0191 − ガス$0.0082 = 純利益+$0.0109
```

**見積もりの粗利 $0.0191 と、チェーン上で確定した粗利 $0.0191 が一致した。**
9月18日の失敗6件は −3〜−94bps ずれていたので、ここが直ったことになる。
経路の型も、成功していた頃と同じ「V2でずれた直後にV3(0.05%)で戻す」だった。

### 作り置きを止めた前後(同じ構成、8分ずつ)

| | 作り置きあり | **作り置きなし** |
|---|---|---|
| 価格表 | 334/616 | **39/616** |
| 経路計算 | 53,076 | 40,973 |
| **粗利プラス** | **7** | **89** |
| 精査 / 黒字 | 7 / 0 | **59 / 34** |
| 実行 | 0 | **1(成功)** |
| RPCの呼び出し | 毎分41 | **毎分27** |

**価格表を88%減らして、粗利プラスが12倍になった。**
作る表が「ふるいが指した経路の段」だけになったため、
**評価する経路の密度が上がった**ということ。

要求は `要求で作成25/25` と**取りこぼしゼロ**。待ち行列も常に0だった。

### 残っている制限

- **黒字34件のうち32件が `下限`**(MIN_PROFIT_USD=$0.01 未満)で見送られている。
  取れる機会の大半は$0.01に届かない規模
- RPCの主役は**受信**(毎分227)で、呼び出し(毎分27)ではない。
  受信は監視プール数に比例するので、広げるときはここを見る
- `[ふるいの実物]` の首位は相変わらず「64%の価格差が動かない」もので、
  金額の条件は通るが `simulateRoute` まで行っていない。
  **税トークンかハニーポットの可能性が高いが、まだ断定していない**

## 「ずれが消えた」は早すぎた。原因は手数料(2026年9月19日)

成功1件を見て「見積もりのずれが消えた」と報告したが、**早すぎた**。
件数が増えたあとの実測はこうだった。

```
[判定の精度] 送信直前でも黒字 2件 / 赤字と確定 13件(精度13%)
```

**成功2件(+$0.0109、+$0.0140)に対し、13件がチェーン上では赤字。**
ただし**1円も失っていない**。13件すべて `simulateRoute` が送信前に止めている。
守り(Revert protection)は設計どおり働いた。

### 13件のうち12件が同じDEX

```
[実行] uniswap-v3(0.05%)→dystopia: 赤字(-17.5bps、見込み$0.0138)
[実行] uniswap-v3(0.05%)→dystopia: 赤字(-20.6bps、見込み$0.0182)
[実行] dystopia→uniswap-v3(0.05%): 赤字(-15.9bps、見込み$0.0154)
…
```

**誤差は −11.6 〜 −20.6bps。投入額($9.20〜$16.00)によらずほぼ一定で、
向きにもよらない。**

**幅が一定ということは、深さの計算ではなく手数料の値が違うということ。**
深さの誤差なら投入額とともに広がるし、向きで変わる。
壁0.35%(V3 5bps + V2 30bps)という前提が、実際より**約16bps低かった**。

### なぜ直らなかったのか

`feeProbed`(手数料は実測済み、という印)は**プール地図に保存される**。
一度 true になると二度とやり直されない。生存ログの `手数料0(残0)` が
まさにその状態で、**手数料の実測は1件も走っていなかった**。

### 直したこと

送信直前の確認で**同じプールが2回続けて赤字**になったら、
そのV2プールの `feeProbed` を外す(`REPROBE_AFTER_REJECTIONS`、既定2)。

- 判定はただちに安全側の `UNPROBED_FEE_BPS`(既定45bps)に戻る
- あわせて手数料の実測待ち行列に入り、実際の値が測り直される
- V3は手数料帯が確定しているので対象外

`[手数料の見直し]` 行で、どのプールがいつ外れたかが分かる。

### 教訓

**「精度が直った」を1件で言わない。** `[判定の精度]` は分母が増えてから読む。
成功1件の時点では 1/1 = 100% に見えていた。

## 手数料の見直しを入れた後の実測(2026年9月19日 02:10〜05:48 UTC)

```
[判定の精度] 送信直前でも黒字 12件 / 赤字と確定 6件(精度67%)
[生存] 経路計算1,665,246→粗利プラス9,776 精査6,892 黒字1,261 実行12/1
       内訳[冷却19 下限1155 送信中26 見送5] 失敗段階[estimateGas:1]
```

**精度 13% → 67%。取引12件が成立し、失敗0件(1件は送信前のガス見積もりで中止)。**

| 時刻(UTC) | 純利益 |
|---|---|
| 02:30 / 02:31 | +$0.0253 / +$0.0242 |
| 03:40 | +$0.0164 |
| 04:11 | +$0.0166 |
| 04:48 ×3 | +$0.0121 / +$0.0214 / +$0.0134 |
| 04:49 / 04:50 | +$0.0125 / +$0.0135 |
| 05:16 ×3 | +$0.0159 / +$0.0281 / +$0.0355 |
| **合計12件** | **+$0.2349** |

前のデプロイの2件(+$0.0249)と合わせて **14件・+$0.2598**。

### 手数料の見直しは本番で発動した

```
[手数料の見直し] polygon 0x03daa61d…:0x6D3842ab… が2回続けてチェーン上で赤字。
                 手数料30bpsの前提を外し、実測し直します
```

このプールを含む経路は直前に `純利益+$0.0285` `+$0.0416` `+$0.0370` と
出ていたが、いずれも送信直前で赤字だった。**狙いどおり、繰り返す嘘を
自動で止められている。**

### ガス代の見積もりが約40%過大

| | 見積もり | 実際 |
|---|---|---|
| 代表値 | $0.0107〜0.0139 | **$0.0073〜0.0090** |

過大な分だけハードルが高くなり、取れる機会を捨てている。**次の改善候補。**

### いちばん大きな取りこぼしは「下限」

内訳の `下限1155` が突出している(黒字1,261件のうち1,155件)。
実際のガス代が$0.0074〜0.0090なので、`MIN_PROFIT_USD=$0.01` は保守的すぎた。
**2026年9月19日に $0.005 へ引き下げた。**

次点は `送信中26`(同じチェーンで送信が重なり見送った分)。

## ガス代の見積もりが過大だった(2026年9月19日に修正)

実測すると、事前の見積もりが実際に払った額より一貫して高かった。

| 見積もり | 実際 | 倍率 |
|---|---|---|
| $0.0107 | $0.0074 | 1.45 |
| $0.0125 | $0.0082 | 1.52 |
| $0.0139 | $0.0090 | 1.54 |

**過大な分はそのままハードルの高さになり、本物の機会を赤字と判定する。**
原因は2つあった。

### ① 上限(gasLimit)を費用として数えていた ← 明確な誤り

```js
const gasWithBuffer = (gasUnits * 120n) / 100n;
const measuredGasUsd = await gasUnitsToUsd(chain, gasWithBuffer);  // ← これ
```

`gasWithBuffer` は**送信時の上限**として渡すもので、余裕を持たせるのは正しい。
しかし **EVMは使わなかったガスを請求しない**ので、払うのは `gasUnits` の分だけ。
余裕の20%をそのまま費用に足していた。費用の計算は `gasUnits` に直した。

### ② 単価が実際より高い(残り約27%)

`baseFeePerGas + maxPriorityFeePerGas` は「送信時にこれだけ出す用意がある」
額で、実際にブロックへ入る時の実効単価はそれより低いことが多い。
ガス使用量はほぼ的中している(285,000 対 285,579)ので、ずれているのは単価。

そこで **receipt の実効単価と見積もりの比を覚えて、次から掛ける**。
`[ガス単価の学習]` 行に出る。

**安全のための歯止め**: 比は **0.7〜1.0** に収める。
**見積もりを実際より低くする方向(1.0超)には振らない**し、7割より下げもしない。
ガスが急に上がった場面で過小評価して赤字を出さないため。
件数で重みを付けた平均なので、1件の外れ値で大きく動かない。

### 確認したこと

| 入力(実際/見積もり) | 結果 |
|---|---|
| 0.66 | **0.700 で止まる**(下限) |
| 0.80 | 0.750(2件の平均) |
| **1.30** | **1.000 で頭打ち**(高い側へは振らない) |
| 0 を渡す | null で無視 |
| 未知のチェーン | 1.0(補正なし) |

## 「黒字判定の合計$107」は取り逃した金額ではない(2026年9月19日)

ダッシュボードの24時間の記録簿:

```
記録件数4,725 / 黒字判定の合計+$107.550 / 実際に得た利益+$0.7707 / 大口取引の検知57
内訳: 最低利益未満4,164 / 送信直前に見送り329 / competitor_check 185 / 成功40 / 送信失敗7
送信まで進んだ判定額: +$0.6395
```

「黒字と判定したのに取れなかった上位」8件は、**全てチェーン上では赤字**だった。

| 投入 | 判定額 | 実測 |
|---|---|---|
| $2,000 | +$27.99 | **−57.8bps** |
| $460(3段) | +$19.47 | **−476.7bps** |
| $1,200 | +$5.58 | **−67.5bps** |
| $391 | +$0.42 | −9.5bps |
| $11.96 ×3(dystopia) | +$0.25 | −105〜−113bps |
| $30 | +$0.24 | −69.8bps |

**送っていれば利益どころかガス代を失っていた。** `+$107.550` は同じ経路の
再検知を何度も足した値で、取り逃した金額ではない。

### 一方で、良い数字も出ている

```
送信まで進んだ判定額: +$0.6395
実際に得た利益:      +$0.7707
```

**予測より20%多く儲かっている。** ガス代を過大に見積もっていた分で、
同日の修正(最大30%削減)が正しい方向だった裏付け。

**成功40件の投入額は$1.70〜$16.00。失敗の上位3件は$460〜$2,000。** 桁が違う。

## 段ごとの答え合わせ(2026年9月19日に追加)

今までは**経路全体で何bpsずれたか**しか分からず、−9.5bps から −476.7bps まで
並んでいても**どの段が嘘をついているか特定できなかった**。
原因を推測で決めるしかなく、実際この日に2回外している
(手数料の件、異常の上限1000bpsの件)。

そこで:

- `finalize` が**段ごとの見込み**(`legAmounts`: いくら入れていくら返ると
  見込んだか)を経路に残す
- 赤字と確定した時だけ、**同じ投入額で各段を単独に正確に見積もり直す**。
  V3は自前の `quoteV3`、V2は準備量を読み直して同じ式で再計算
- 見込みと突き合わせ、`[段ごとの答え合わせ]` 行に段ごとの誤差を出す。
  いちばん悪い段が `DIAGNOSE_MIN_BPS`(既定5bps)を超えていれば**名指しする**

```
[段ごとの答え合わせ] polygon uniswap-v3(0.01%)→sync発見 投入…:
  1段目 uniswap-v3(v3) -2.1bps / 2段目 sync発見(v2) -474.0bps
  ← 2段目 sync発見:0x7e2eaa… が原因
```

**RPCはほぼ増えない。** 赤字の確定は1日329件で、1件につき最大2回の
束ね呼び出し。月約2万回は枠2,000万の0.1%。

### 確認したこと

- 誤差の計算: 見込み1000/実際950 → −500bps、同値 → 0bps、見込み0 → null
- **V2の再計算式が判定側(`getAmountOutV2`)と完全に一致**。
  つまり準備量が変わっていない段は必ず0bpsと出る(犯人でない段が
  ノイズで名指しされない)

## 記録簿の時刻が9時間ずれていた(2026年9月19日に修正)

```js
new Date(e.timestamp).toLocaleString('ja-JP')   // ← 書式だけ日本式、時刻はUTC
```

`toLocaleString` の第1引数は**書式**しか決めない。時刻そのものを日本時間に
するには `timeZone` を渡す必要がある。渡さないとコンテナの設定(UTC)のまま。

**日本式の書式で9時間ずれた時刻が出ていたため、かえって気づきにくかった。**

### 直したこと

- `formatLocalTime()` を追加し、`timeZone: DISPLAY_TIMEZONE`(既定 `Asia/Tokyo`)で整形
- **列の見出しに「日時(日本時間)」と明記**。書式だけでは判別できないため
- 桁を2桁固定にし、年を省いて `09/19 14:16:56` の形に(iPhoneの狭い列で折り返さないように)
- 不正な値や `null` は `-` を返す。タイムゾーンが使えない環境では UTC 表記に落ちる

### 検算

| 記録(UTC) | 画面(日本時間) |
|---|---|
| 09-18 21:42:42 | **09/19 06:42:42**(日付をまたぐ) |
| 09-19 02:30:38 | 09/19 11:30:38 |
| 09-19 05:16:56 | 09/19 14:16:56 |
| 09-19 15:30:00 | **09/20 00:30:00**(日付をまたぐ) |

なお**生存ログなどの標準出力の時刻はRailway側の表示**なので、ここでは変えられない
(Railwayの画面はUTC)。

## ガス代の見積もり: 原因は単価ではなくガス量だった(2026年9月19日、実測で訂正)

修正後の実測。

```
[実行] 確定: 粗利+$0.0248 − ガス$0.0074 = 純利益+$0.0174(見積もりガス$0.0094)
[ガス単価の学習] polygon: 今回 実際/見積もり=1.000 → 補正1.000(実測1件)
```

| | 修正前 | 修正後 |
|---|---|---|
| 見積もり / 実際 | $0.0125 / $0.0082 = **1.52** | $0.0094 / $0.0074 = **1.27** |

**上限(gasLimit)を費用に数えていた分(×1.20)は消えた。**

### 私の見立ては外れていた

残りの27%を「単価が高い」と見ていたが、`[ガス単価の学習]` の
**実際/見積もり=1.000**(=実際の実効単価は見積もり以上)がそれを否定した。
**単価は合っている。残っているのはガス量のずれ。**

`estimateGas` が返す値は、実際に使われる `gasUsed` より多めに出る。
送信直前の費用はこの `estimateGas` の値をそのまま使っているため、
その分だけ過大になる。

**次の候補**: 送信直前の費用にも、実測の平均(`getAverageGasUnits`)を
使うか、`estimateGas` と実測の比を同じやり方で学習する。
ただし**事前判定(ハードル)側はすでに実測の平均を使っている**ので、
`粗利プラス → 黒字` の通過率への影響は小さい。優先度は高くない。

## 答え合わせが旧コントラクトのチェーンで使えなかった(同日に修正)

```
[段ごとの答え合わせ] arbitrum uniswap-v3(1.00%)→uniswap-v3(0.05%):
  1段目 uniswap-v3:読めず / 2段目 uniswap-v3:読めず
```

Arbitrum と Avalanche のコントラクトは **quoteV3 の無い旧版のまま**
(再デプロイ未実施)なので、自前の見積もりが1件も返らない。

**対策**: 読めなかったV3の段だけ、公式の QuoterV2 で埋める。
公式ファクトリーのプールなら引けるため、Arbitrum の Uniswap V3 は
これで読める。**フォークは対象外**(公式に投げると別のプールの価格が返る)。

## 価格表を保存して起動時に戻す(2026年9月19日)

作り置きをやめてから、**再デプロイのたびに価格表がゼロに戻り、判定が
立ち上がるまで20〜40分かかる**ようになった。この日の計測は何度もその
空白に当たって読めなかったし、その間は取引の機会も取れない。

### 危険なので、条件つきでしか戻さない

古い価格表をそのまま使うのは危険で、**それが9月18日の幻の利益の正体**だった。
そこで**表を作った時の価格も一緒に保存し、起動時に今の価格と比べる**。

- プールが**まったく動いていなければ**、その表はいま作っても同じ → 戻す
- 少しでも動いていれば**捨てる**(作り直しは要求が来た時に走る)
- 基準の価格も**当時の値**で戻すので、ズレの積算がやり直しにならない
  (`setQuoteBase` を追加。`markQuoteBase` は「今の価格」を基準にするので使えない)
- **6時間より古い表は、価格が動いていなくても捨てる**
  (`QUOTE_TABLE_MAX_AGE_HOURS`)。価格が同じでも、流動性の出し入れで
  曲線そのものが変わっているため

保存先はプール地図と同じ場所(`POOL_MAP_FILE` の隣)。`QUOTE_TABLE_FILE` で変えられる。
保存はプール地図と同じ5分ごと。

```
[価格表の復元] 37本を戻しました(0.3時間前の保存) / 価格が動いていて破棄12 / 古すぎ0 / 対象なし3
```

### 確認したこと

| 確かめたこと | 結果 |
|---|---|
| 書き出し→取り込みの往復 | **桁落ちなし**(BigIntを文字列で保存) |
| 動いていない | 復元する |
| 0.2bps / 2bps 動いた | 復元する(閾値3bps以内) |
| **20bps 動いた** | **破棄する** |
| **200bps 動いた** | **破棄する** |
| 基準の価格 | **当時の値のまま戻る** |
| 壊れた値・空の配列 | 弾く |

## 【障害】画面の変数ミスで bot が落ち続けた(2026年9月19日)

```
ReferenceError: tzLabel is not defined
    at renderPage (file:///app/index.js:1679:43)
```

日時の見出しに `${tzLabel}` と書いたが、**その変数を定義したのは別の関数
(生存ログ側)の中**だった。同じ `const mc = getMulticallStats();` という行が
2つの関数にあり、置換が先に出てくる方(生存ログ)に当たっていた。

### 被害が大きかったのは、捕まえる人がいなかったから

```js
http.createServer((req, res) => {
  res.end(req.url === "/about" ? renderAbout() : renderPage());   // try/catch なし
})
```

`renderPage` が投げた例外を誰も捕まえないので、**ページを開くたびに
プロセスごと落ちた**。裁定の判定も送信も道連れになり、再起動のたびに
価格表の作り直し(20〜40分)からやり直しになっていた。
HTTPログは 08:53 から 502 「connection refused」が続いていた。

### 直したこと

1. `TZ_LABEL` をモジュールの定数にした(どの画面からも使える)
2. **リクエストの処理を try/catch で囲んだ。** 画面は「あれば便利なもの」で
   bot の本体ではない。壊れても 500 を返すだけにして、判定と売買は続ける

### 教訓

- **同じ行が複数の関数にある時、機械的な置換は先頭に当たる。**
  変数を足したら「使う場所から見えるか」を必ず確かめる
- **`node --check` は構文しか見ない。** 未定義の変数は通る。
  画面を変えたら**実際に起動して開く**(ローカルで `curl` すれば足りる)

### 確認したこと

ローカルで起動して実際に開いた。

| 確かめたこと | 結果 |
|---|---|
| `/` | **HTTP 200**(9,789バイト) |
| `/about` | **HTTP 200**(5,088バイト) |
| 見出し | **日時(日本時間)** |
| リクエスト後のプロセス | **生存**(ReferenceError 0件) |

## 同じチェーンで並行して送る(2026年9月19日)

### 何を直したか

24時間の記録簿で **「送信中」で見送った機会が26件**。成功が40件なので、
見送りは成功の6割に相当する。機会は数分間に集中して来る
(6件が3分間に来た実測がある)ので、「同じチェーンでは一度に1件」の
直列送信だとその山を取りこぼしていた。

直列にしていた理由は nonce の取り合い(2件が同じ番号を取り、片方が
"replacement fee too low" で失われ、粗利$0.4155を丸ごと失った)。

- **ethers の `NonceManager`** で nonce を手元で数える。同時に送っても番号が
  重ならない(同時3件で 7,8,9 と配られることを検証済み)。署名者はチェーン
  ごとに1つを使い回す
- 送信や確定待ちに失敗したら **`reset()`** で鎖上の値に合わせ直す。
  送れなかった番号が手元に残ると、以降の送信が全部詰まるため
- チェーン単位の錠を **プール単位の錠** に変えた。同じプールを使う経路を
  同時に送ると、先に着いた方が価格を動かして後の方が巻き戻り、ガス代だけ
  失うため。**別のプールなら並行して送る**
- 同時に飛ばす本数の上限 `MAX_PARALLEL_SENDS_PER_CHAIN`(既定3)。
  1本が詰まった時の被害を限るため

### 見方

生存ログの `送信中N`(内訳)が減れば効いている。増えていれば
プール単位の錠が効いている(同じプールに集中している)ということ。
`失敗段階[send:…]` が増えたら nonce の問題を疑い、上限を1に戻す
(`MAX_PARALLEL_SENDS_PER_CHAIN=1` で従来と同じ直列になる)。

## 【飛躍の本命】Optimism / Base の再開手順(オーナー立ち会いが必要)

HANDOVERの実測どおり、**手数料の壁が Polygon の35bpsに対し 6bps、ガスも最安**。
$5投入時のハードルは Polygon 59bps に対し **Optimism 16bps**。
同じ価格差の分布なら、**届く経路が数倍**になる。

止めていた理由は「未監視ファクトリーの価格表が作れない」ことだったが、
自前の `quoteV3`(フォーク対応)と Algebra 対応で**解消済み**。
残っているのは**チェーンごとの準備**だけ。

### 手順(1チェーンずつ。Optimism → Base の順)

1. **RPC を用意**: Chainstack で Optimism のノードを追加し、
   `OPTIMISM_RPC_URL` と `OPTIMISM_WSS_URL` を Railway に設定
   (URLは秘密情報。リポジトリには絶対に書かない)
2. **コントラクトをデプロイ**: `RUN_MAINNET_DEPLOY=optimism` で起動。
   ログに出た住所を `MAINNET_CONTRACT_ADDRESS_OPTIMISM` に設定し、
   `RUN_MAINNET_DEPLOY=false` に戻す。デプロイ用のガス(ETH)が
   bot ウォレットに要る(Optimism は数十セント)
3. **稼働に加える**: `ACTIVE_CHAINS=polygon,arbitrum,avalanche,optimism`
4. **フォークを有効化**: `ENABLE_FORK_QUOTER=polygon,optimism`
5. **確認**: `[発見] optimism univ3-fork-d(uniswap): V3プール○件` が0件で
   ないこと、`[準備完了] optimism` の監視プール数、`[判定の精度]`

**注意(点検で判明)**: V3プールは起動ごとに探索されるが、**V2プールは
プール地図の再構築時にしか集めない**(保存済みの地図があると週1回)。
新しいチェーンを加えた直後は Optimism の V2 が地図に無いため、始点の拡張
(深さで選ぶ)も V2↔V3 の経路も、まず手書きの5トークン分だけになる。
初回だけ `MAP_REBUILD_AFTER_HOURS=0` で起動して地図を作り直し、終わったら
`168` に戻す(再構築は全チェーンぶんで数百回のRPCを使う。枠には十分収まる)。

Arbitrum / Avalanche の**再デプロイ**も同じ手順(手順2だけ)。
これで両チェーンのフォークのプールと、段ごとの答え合わせが使えるようになる。

## Optimism を再開した(2026年9月19日 13:45 UTC)

手順どおりに進めた結果。

| 段階 | 結果 |
|---|---|
| Chainstack ノード追加 → `OPTIMISM_RPC_URL` / `OPTIMISM_WSS_URL` | 設定済み |
| bot ウォレットへ ETH 入金 | 0.00257 ETH(残高 0.00506 ETH) |
| `RUN_MAINNET_DEPLOY=optimism` | **完了: 0xD2D45cC99AAe1AF7302b067d116fEEA8d7ceAca1**(1回で成功、`false` に戻した) |
| `ACTIVE_CHAINS` に追加、`ENABLE_FORK_QUOTER=polygon,optimism`、初回 `MAP_REBUILD_AFTER_HOURS=0` | 起動後に `168` へ戻した |

起動ログ:

```
[発見] optimism univ3-fork-d(uniswap): V3プール27件        ← 9/17の調査と一致。書き写しの誤りなし
[準備完了] optimism: V2 0件 / V3 57件(中身が空のV3 8件を除外)、57プールを監視します
[オンチェーン] optimism: 57プールを1回の購読で監視します    ← WSS 接続、受信開始
[状態] optimism: 判定可能ペア 9/9 / 使えるプール V3 57/57
```

始点は 36 → **45種**、監視プールは 1,143 → **1,303**。RPC の月末見込は直後で 17%(変化なし)。

### 予告どおりの制限

**V2 は 0件。** 種プール(verified pairs)に Optimism が無いため、地図の再構築でも
Optimism の V2 は集まらなかった。V2↔V3 の経路は当面組めず、**V3↔V3 の経路のみ**
(HANDOVER の実測では Optimism の活動の 97.6% が V3 なので、影響は限定的)。
V2 を足すには、Optimism の V2 ファクトリー(Velodrome 等)の種プールを
verified pairs に入れる必要がある。

### これから見るもの

- `[判定の精度]` と `実行` に **optimism** が現れるか。壁が 6bps なので、
  Polygon より小さい価格差で黒字になるはず
- `枠[… 月末見込N%]`。Optimism の受信が乗る。20%台なら問題なし
- `[発見] optimism` が今後の起動で 0件になったら RPC/WSS の疎通を疑う

## 一晩の漏斗の点検(2026年9月20日、JST 1:00〜9:40 の実測)

「利益が出ない夜に、自分で機会を捨てていないか」を数字で切り分けた。

| 段階 | 件数 | 判定 |
|---|---|---|
| 黒字判定 | 348 | |
| ├ 下限($0.005未満) | 190 | 捨てているが、全部本物でも合計$0.5未満。本命ではない |
| ├ 送信中(同じプール使用中) | 59 | 同じ経路の再検知が大半。送信は5件だけで上限3本には当たっていない |
| ├ 冷却 / 見送 | 48 / 11 | 正常 |
| ├ 送信直前で赤字 | 12 | **精度25%**(前日67%) |
| └ **送信 → 失敗** | **5 / 成功0** | **ここが本当の損** |

失敗5件: Arbitrum 2段(送信前に状況変化)、Arbitrum 2段 $276(巻き戻り)、
**Polygon 3段(制限時間超過)**、Avalanche 3段 ×2(巻き戻り)。
巻き戻りは送信から確定までの数秒で状態が動いたもの(競争または遅延)で、
ガス代だけ失う。3段と新しいチェーンに集中している。

### 見つけた自分のバグ: 制限時間超過で nonce を合わせ直していなかった

並行送信(NonceManager)は send / wait の失敗で `reset()` するが、
**20秒の制限時間超過は index.js の Promise.race で起きるためそこを通らない**。
手元の nonce が1つ進んだまま残り、その送信が未確定のまま消えると、
以降の送信が全部その番号待ちで詰まる。旧コードは送信のたびに nonce を
読み直していたので自然に直っていた。
→ `resetNonce` を公開し、index.js の失敗処理(制限時間超過を含む)からも呼ぶ。

### 赤字12件の犯人は「同じプール」

| 犯人の段 | 件数 | 誤差 |
|---|---|---|
| V2(dystopia / sushiswap / sync発見) | 3 | −40〜−57bps |
| V3 Optimism(uniswap-v3、`0xc1738D90…` が2回) | 4 | −15〜−34bps |
| V3 Arbitrum | 1 | −20bps |

→ **答え合わせで一定回数(既定2回/6時間以内)犯人になったプールは、
一定時間(既定60分)そのプールを含む経路を判定しない**(`notePoolBlame` /
`isPoolQuarantined`、`[一時除外]` 行)。犯人の段は「同じ式・同じ手数料で今の
状態を読み直しても見込みと合わない」プールなので、外しても本物の機会は
減らない。環境変数 `BLAME_QUARANTINE_AFTER` / `BLAME_QUARANTINE_MINUTES` /
`BLAME_WINDOW_MINUTES`。

### まだ分かっていないこと

- V2 の段が −40〜−57bps 一定でずれる原因(受信の抜けか、Solidly系の
  Sync 識別子の違いか、競争か)。一時除外は症状を止めるだけで原因ではない
- Optimism の V3 が −15〜−34bps ずれる原因。壁6bps の効果はまだ測れていない
  (`[惜しい] optimism` では112本中40本が粗利プラス、38本が −5bps 以内と
  **分布は明らかに良い**。精度が戻れば化ける可能性がある)

## 過去の誤り(再発防止)
- イベント識別子を手書きして1文字欠け、最初期から一度も受信できていなかった → ethers.id()で計算する
- RPCにタイムアウトが無く14時間凍結 → 全呼び出しに上限を設けている
- GitHubのコントラクトが修正前の版のままデプロイされ、全取引が必ず失敗していた → デプロイ前に中身を確認する
- V3価格表がWebSocketの無いチェーンで作り直されず、幻の黒字を毎分出していた
- 背景の手数料実測が必ず失敗するループでRPC枠を浪費していた
- 各段5bpsの安全余裕とbot側の見積もり誤差で、本物の機会を赤字と判定していた
- 手数料実測の再試行待ち(直近取引なしで24時間)を呼び出し側が見ておらず、
  待機中のプールを毎秒キューに積み直していた。RPCは消費していないが
  「未実測○プールを確認します」が2秒ごと・1日約43,200行出て、[生存] や
  [機会] の行が流れていた → isFeeProbeOnHold() でキューに入れないようにした

## コントラクトのガス削減(2026年9月20日に実装し、同日に4チェーンとも再デプロイ)

再デプロイの記録(JST 12:50〜13:00、Avalanche → Polygon → Optimism → Arbitrum の順。
`RUN_MAINNET_DEPLOY=<次のチェーン>` と前のチェーンの住所を1回の変数更新にまとめ、
再起動を5回に抑えた): 住所は「稼働環境」の節。Optimism だけ RPC が "already known"
(同じ取引の二重受信)を返して台本が失敗扱いになったが、取引は送られていた。
住所は送信者と nonce から計算し(前日の Optimism 展開が nonce 7 = 既知の住所で
一致することも確認)、起動時の `[コントラクト]` ログで中身を裏付けた。
この件で、起動時に全チェーンの版を確かめるログ(`checkContractVersions`)を足した。
旧コントラクトの利益の回収は未実施(所有者キーで withdraw を直接呼ぶ)。

取引頻度が上がらない主因は「1件の粗利($0.01〜0.04)に対してガス代($0.015)が
同程度」であること。2段の実測 272,246 ガスの内訳を実測して削った。

### 計測のやり方(この砂場は鎖の RPC に届かないため)

ローカルの EVM(anvil)に**実物の Uniswap V2 pair / V3 pool のバイトコード**
(npm の @uniswap/v2-core, v3-core)と簡易ERC20を置き、現行と改良案を同じ
経路で実行して gasUsed を比べた。各経路は1回ならし運転してから2回目を測る
(V2の累積価格やコントラクトの残高が0でない、本番と同じ状態にするため)。
計測台と試験は砂場(scratchpad)に置き、リポジトリには入れていない。

### 分かったこと: 主犯は永続記憶への印の書き込み

現行は `activePool` / `activePayToken` / `inFlashSwap` を永続記憶に書く。
1件の取引で 0→値→0 と書き戻すため、**書き込み約65,000ガス、払い戻し約60,000ガス**。
EVM は払い戻しを**取引全体の2割まで**しか認めない(EIP-3529)ので、本番
(払い戻し前 約340,000)では上限 68,000 に当たり、約12,000 が消えていた。
これが「estimateGas が実測より27%多い」の正体でもある(見積もり 345,000 =
払い戻し前 / 実測 272,246 = 払い戻し後)。

### 変更(contracts/DexArbFlashLoan.sol)

| # | 変更 | 削減(2段 V2→V3、本番見込み) |
|---|---|---|
| 1 | 印を一時記憶(`transient`、EIP-1153)に。取引の終わりに勝手に消える | 約15,000 |
| 2 | `token0()` を鎖上で問い直さず、`flags` で受け取る | 約3,500 |
| 3 | `getAmountOut` の試し呼びをやめ、持つプールだけ `flags` で指定 | 約1,000 |
| 4 | V3の段は残高で測らず、プールが返す量(delta)を使う | 約1,600/段 |
| 5 | `owner` を immutable に | 約2,100 |
| 6 | Leg から `tokenIn` を外す(前の段の tokenOut と同じ)。呼び出しデータ 484→420バイト | 約700 |
| 7 | optimizer runs 200 → 1,000,000、evmVersion cancun を明示 | 約1,300 |
| | 合計 | **約21,000〜22,000(約8%)**。Polygon で $0.015 → 約$0.0138 |

残したもの(資金の安全のため): 最終段の資産残高の差分(税トークンで自分の
残高から返済してしまう事故を防ぐ)、V2の段の到着量確認、`RouteExecuted`。

計測台の値(実物の Uniswap のバイトコード、ならし運転後):

| 経路 | 現行 | 一時記憶のみ | 全部 |
|---|---|---|---|
| V2→V3(2段) | 196,927 | 159,698 | 151,924 |
| V3→V2(2段) | 165,652 | 157,429 | 149,080 |
| V3→V3(2段) | 187,370 | 168,553 | 159,659 |
| V2→V3→V2(3段) | 248,957 | 219,354 | 207,875 |

4経路とも利益の値が現行と完全に一致した。計測台は本番より軽い(トークンが
簡易ERC20で、本番は USDC の代理コントラクトやティック越えが乗る)ため、
払い戻し上限の効き方が違い、**本番の見込みは上の「約8%」の方**。

### 再デプロイ後の実測(2026年9月20日 JST 21:25〜21:43、取引3件・失敗0・精度100%)

| チェーン | 経路 | 投入 | 実測ガス量 | 粗利 | 実ガス代 | 純利益 |
|---|---|---|---|---|---|---|
| avalanche | 2段 sushiswap→uniswap-v3(0.30%) | $5.98 | **206,730** | $0.0208 | $0.0091 | **+$0.0117** |
| polygon | 2段 sync発見→algebra-a(0.30%) | $4.60 | 545,203 | $0.0183 | $0.0170 | +$0.0013 |
| polygon | 2段 sync発見→algebra-a(0.30%) | $4.60 | 258,117 | $0.0196 | $0.0091 | +$0.0105 |

**V2→V3 の2段は 206,730 ガス**。旧版の実測(256,409〜272,246、Polygon)と比べて
**約2割少ない**(見込みの8%を上回った)。ただし別チェーン・別プールなので厳密な
比較ではない。Algebra を含む経路は重く、同じ経路でも 545,203 と 258,117 と
**2.1倍の開き**が出た(V3の価格帯をまたぐ回数で変わる)。

この開きを受けて、事前判定のガス量を**平均から中央値**に変えた
(`getAverageGasUnits`)。平均だと1件の外れ値でハードルが約$0.005 上がり、
これは最低利益と同じ大きさで本物の機会を捨てることになる。

### 正直な見立て

2段の約20万ガスは Uniswap のプールとトークン側の処理で、こちらのコントラクト
自身は4〜5万ガスしか使っていない。**8%が上限に近く、「粗利 ≒ ガス代」の
構造はコントラクトでは変わらない**。構造を変えるのは、この再デプロイで開く
Avalanche(ガス$0.001)の対象拡大、投入額の最適化、Optimism の費用に L1 データ
手数料が入っていない疑いの確認(別件)。

### Pharaoh 対応(同じ再デプロイに含めた)

Pharaoh(Avalanche の出来高の約78%)は Ramses のフォークで V3形式の `swap` を
持つ。コールバックの名前は公開資料から確定できなかったので、名前に依存しない:
- `ramsesV2SwapCallback` を名前つきで追加(Ramses 本家の名前)
- **fallback を「今スワップ中のプールの形式で解釈」**するよう拡張。V3形式なら
  `(int256,int256,bytes)`、V2形式なら `(address,uint256,uint256,bytes)`。名前が
  何であれ受け付けるので、今後のフォークにも再デプロイ不要
- `V3_FACTORIES.avalanche` に Pharaoh のファクトリー `0xAAA32926…`(Snowtrace の
  表示名「Pharaoh Exchange: Factory V2」、fork: true、dexId `pharaoh-cl`)を追加。
  **2026年9月20日の再デプロイ後、`ENABLE_FORK_QUOTER` に avalanche を足して検証済み**:
  `[発見] avalanche pharaoh-cl(uniswap): V3プール9件`(univ3-fork-e 9件、univ3-fork-f 16件)。
  Avalanche の監視は V2 40件 / V3 41件の79プールになった
- 調査で42%を占めた `0x1128F23D…`(univ3-fork-e)は **Pangolin V3**(Uniswap V3形式)
  だった。こちらも同じ再デプロイで開く

### bot 側の変更

- `scripts/execute-opportunity.js`: 新旧2つの ABI を持ち、`detectContractVersion` が
  `FLAG_V3()` の eth_call で判別して覚える(新版は 1、旧版は fallback が
  "unknown call" で取り消す)。RPC の失敗(取り消し以外)は覚えず、その機会だけ
  見送る。`buildLegArgs` が版に応じた形を作る。`flags` の token0 は地図(pool-registry)
  から、getAmountOut の有無は手数料の実測で判明した記録(`poolHasAmountOut`)から
- `scripts/onchain-reserves.js`: getAmountOut で手数料が取れたプールに印
  (`byAmountOut`)を残し、`poolHasAmountOut` で読めるようにした
- `scripts/compile-contract.js`: runs 1,000,000、evmVersion cancun(展開サイズ 14,615バイト)
- `quoteV3` の引数と `SimulationResult` / `RouteExecuted` は旧版と同じ。
  `quoteV3ByPoolBatch`(multicall-reserves.js)は変更なし

### 一時記憶が使えるチェーンの確認

| チェーン | 根拠 |
|---|---|
| Polygon / Optimism | 現行のバイトコードに既に Cancun の `MCOPY` が含まれ、本番で動いている(cast disassemble で確認) |
| Avalanche | Etna アップグレード(2024年12月16日)で ACP-131(Cancun EIP、EIP-1153 含む)が有効 |
| Arbitrum | ArbOS 20 "Atlas" で TSTORE/TLOAD に対応 |

### 試験(ローカル EVM、18項目合格)

版の判別(新版 `FLAG_V3()`=1、旧版は取り消し)、simulateRoute と executeRoute の
利益の一致、quoteV3(実物の V3 プール)、**名前の分からないコールバックで呼び返す
模型プール**で quoteV3 / 2段目 / 1段目(flash)の全て、部外者からのコールバックと
fallback と executeRoute の拒否、token0 の向きを間違えた flags の取り消し(V2/V3)、
minProfit 未達の取り消し、失敗の前後で残高が変わらないこと、withdraw。

### 再デプロイの手順(オーナー立ち会い)

1. 対象チェーンの旧コントラクトの利益を `withdraw` で回収する(仕組みは未実装。
   所有者キーで直接呼ぶ)
2. `RUN_MAINNET_DEPLOY=<チェーン名>` で起動 → 表示された住所を
   `MAINNET_CONTRACT_ADDRESS_<チェーン>` に設定 → `RUN_MAINNET_DEPLOY=false`
3. 起動ログの `[コントラクト] <チェーン> …: ガス削減版(flags) と判別しました` を確認
4. Avalanche なら `ENABLE_FORK_QUOTER` に avalanche を足し、探索ログで Pangolin V3 /
   Pharaoh のプール数を確認する
5. 最初の取引の `[実行] 完了: … ガスN` で削減幅を実測し、この節に追記する

## Optimism の費用に L1 データ手数料を入れた(2026年9月20日)

OP Stack(Optimism / Base)は、L2 の実行費(gasUsed × 単価)とは別に、取引の
データを L1 に書く費用を取引ごとに取る。receipt の `l1Fee` に出るが、ethers の
`receipt.gasUsed × receipt.gasPrice` には入らない。bot はこれを数えておらず、
Optimism の費用を実態より低く見積もっていた(L2 の実行費は極小なので、L1 側が
費用の大半になり得る)。Arbitrum は L1 分を gasUsed に折り込む方式なので影響しない。

- 事前判定: 代表的な2段の取引(418バイト)で予備コントラクト GasPriceOracle
  (`0x4200…000F`)の `getL1Fee` に聞いた値を5分だけ覚えて足す。確定した取引の
  `l1Fee` が貯まればその平均を使う(`getTypicalL1FeeWei`)
- 送信直前: 実際の呼び出しデータで作った取引(署名は仮)で聞いて足す(`estimateL1FeeWei`)
- 確定後: 生の receipt から `l1Fee` を読んで費用に足し、`[実行] 確定:` の行に
  「うちL1データ$…」と出す。記録簿には `actualL1FeeUsd` を残す
- OP Stack 以外のチェーンでは全て 0 で、動きは変わらない(鎖に触れない試験で確認)

Optimism の `[惜しい]` が粗利プラス40/112本あるのに黒字が出ない理由の一つが、
費用の過小評価「ではなく」本当にガス代に届いていないのかは、この修正後の
`[生存]` の 内訳[下限] と `[実行] 確定:` の L1 の実額で判断する。

## Optimism で確定前(pending)の状態を使う: Flashblocks(2026年9月20日)

### 背景: 「1ブロック遅い」
bot は WebSocket の logs 購読で動くが、これはブロック確定(Optimism は2秒)まで
届かない。専業の裁定者は確定前の状態で動くので、常に1ブロック遅かった。
OP Stack の Flashblocks は、シーケンサーが 200〜250ms ごとに確定前の部分ブロックを
配る仕組み。Optimism は先着順なので、確定前に検知できれば同じブロックの後ろに
自分の取引が入る。

### 調べたこと(2026年9月20日)
- Polygon は 2026年4月からプライベートメンプールと VeBloP に移行し、未確定取引を
  見る前提が崩れている。FastLane/Atlas のバックラン入札は、未確定取引の配信を
  自前で用意する必要があり(Chainstack は通知1件=1リクエストで枠が数日で尽きる)、
  保証金と 250ms の封印入札で専業5〜8社と競う。安くは直せないので保留
- Arbitrum は Timeboost(優先レーンの入札)が3社で99.7%を独占。シーケンサー配信
  (無料)は数百ms早いだけ。後回し
- Optimism は Chainstack の既存端点が Flashblocks 対応済み。ただし WSS の購読
  (`newFlashblocks` / `newFlashblockTransactions`)は「Invalid params」で拒否され、
  **標準 RPC の pending タグ**として見える形だった(起動時の確認ログ):
  ```
  [Flashblocks/pending] optimism: latest 157148294(29件) / pending 157148295(17件) → … pending 157148295(31件)
    / pendingがlatestより先 5/5回 / 同じpendingの中で取引数が増えた: はい
  [Flashblocks/pending] optimism: eth_getLogs(pending) 72件(ブロック 157148296) / eth_call(pending) 通った
  [Flashblocks] optimism: Flashblocks の購読は拒否(標準の pending タグで読む)。比較用 newHeads の間隔 1899/1998/2125/1911/1949ms
  ```
- Optimism の L1 データ手数料は約$0.000003/件で無視できる(費用に入れる仕組みは入れた)

### 実装(2026年9月20日)
- `dex-onchain-realtime.js`: `FLASHBLOCKS_PENDING_CHAINS`(既定 optimism)のチェーンで、
  監視対象のプールの `eth_getLogs(fromBlock/toBlock = pending)` を `FLASHBLOCKS_POLL_MS`
  (既定 400ms)ごとに取り、確定を待たずに判定へ回す。同じイベントは txHash と logIndex
  で覚え、pending で何度届いても、確定後に logs 購読で届いても、2回目以降は判定に
  回さない。確定後に届いた時に「先読みできた時間」を記録し、`[生存]` の
  `先読み[optimism:事前N件 確定で照合N件 先行平均Nms …]` に出す
- 読み取りも pending に揃える: 準備量と V3 状態と価格表(Multicall3 の `aggregate3`)、
  公式 Quoter、送信直前の `simulateRoute`(eth_call)、ガス見積もり(`eth_estimateGas` を
  pending を明示して生で呼ぶ。ethers の estimateGas はブロックの指定を送らない)。
  他のチェーンは latest のままで動きは変わらない(`readBlockTag`)
- RPC の消費: 400ms 間隔で1日約216,000、月約650万(枠2,000万の3割)。効果を見て
  `FLASHBLOCKS_POLL_MS` を調整する(250ms なら月約1,040万)
- 危険: 確定前の状態は稀に確定しない(取引が落ちる)ことがある。その場合、
  送信した取引が巻き戻ってガス代だけ失う(Optimism のガスは$0.005前後)

### 確かめること
- `[生存]` の 先読み の「先行平均」が数百ms〜1秒台になっているか(確定より早く見えているか)
- Optimism の `[機会]` と送信が増えるか、送信直前の赤字(価格表のずれ)が減るか
- 枠の月末見込が 50% を超えないか

## Optimism を主戦場にする: イベントからのプール発見(2026年9月20日)

### なぜ Optimism なのか(実測)

JST 23:00 台の `[試算]`(壁が下がっていたら取れた金額):

| チェーン | 壁-29bps | 最大の1件 |
|---|---|---|
| **optimism** | 35本 **$29.39** | **$4.42**(投入$2,000) |
| arbitrum | 8本 $14.66 | $4.12(投入$1,600) |
| polygon | 32本 $1.33 | $0.18(投入$60) |
| avalanche | 6本 $0.01 | $0.0013(投入$1) |

Optimism はプールが厚いので、**壁を超えた瞬間の1件が$1以上**になる。Polygon で
毎日$0.01を積んでも1日$50には届かないが、Optimism はそこに手が届く唯一の場所。

先読み(Flashblocks)は既に効いている(確定より平均1.1〜1.2秒早く検知、
3,300件が全件一致)。**速さは足りていて、足りないのは見ている範囲**だった。

### 何が足りなかったか

| | 実測 |
|---|---|
| 監視ペア | **9ペア**(手書きの5通貨の組み合わせだけ) |
| 監視プール | 57(Uniswap 30 + fork-d 27)。**Velodrome が1つも無い** |
| 最小の壁 | 35bps(0.05%+0.30% の組が中心) |
| 5時間の黒字判定 | **0件**。経路194本の最良でも壁を超えず |

### 対策: scripts/pool-scout.js

ファクトリーの住所を当てにせず、**実際に取引が起きているプール**を
Swap / Sync イベントから拾う(アドレス指定なしの eth_getLogs)。

- 正体は `factory / token0 / token1 / fee / tickSpacing / liquidity / getReserves` を
  Multicall3 で一括して読む。`fee()` が無いCLプール(Ramses系・Slipstream系)は
  **tickSpacing から手数料を見積もる**(1→1bps / 10,50→5bps / 60,100→30bps / それ以上→100bps)。
  これは推測なので**低めに倒す**。判定の真偽は価格表(自前の quoteV3)が決めるため、
  ずれても「ふるいを通る/通らない」が変わるだけで嘘の利益にはならない
- 見積もりは自前の quoteV3(プール住所を直接渡す)、コールバックは名前不問の
  fallback。**コントラクトの変更は不要**
- 住所の分からないファクトリーは自前の見積もりが要るので、
  `ENABLE_FORK_QUOTER` が無効なチェーンでは載せない

### 増やしすぎない(RPCの枠を守るため)

プールを増やすと WebSocket の受信がそのまま RPC の消費になる(1件=1リクエスト)。
Optimism は57プールで1日約9万件。無制限に足すと枠2,000万を超えるので、
**今の監視ペアを深くするものだけ**載せる:

| 条件 | 扱い |
|---|---|
| 両方のトークンが手書きの一覧にある | 載せる(同じペアに安い手数料のプールが増え、**壁が下がる**) |
| 片方が一覧にあり、そのペアが既に地図にある | 載せる(比べる相手が増える) |
| 片方が一覧にあり、**取引が最も多い上位**(`SCOUT_ONE_KNOWN_TOP`、既定8位) | 載せる。価格は相手側から導出できるので、**価格の推測は要らない** |
| それ以外(新しいペア) | **載せず、件数と上位だけ報告**。判断してから足す |

### 初回の結果(2026年9月20日 JST 0:41)

```
[プール発見] optimism: 3,000ブロックで337プールが稼働。300件を調べ、43件を地図に追加(V3 16 / V2 27)。
  見送り: 新しいペア(片方だけ既知)182 / 既に地図にある25 / 流動性が0 16 / 新しいペア(両方とも未知)34。RPC18回
[準備完了] optimism: V2 27件 / V3 61件 → 88プール(57から)。経路194本 → 904本
```

**Velodrome の V2 が27件入り、Optimism で初めて V2↔V3 の経路が組めるようになった。**

同時に、**最も取引の多いプール(100分で4,181回)が「片方だけ既知」で見送られていた**
ことが分かった。相手のトークンが手書きの一覧に無いだけで、取引はチェーンで一番厚い。
価格は相手側(USDC等)から自動で導出できるので、価格を推測せずに始点にできる。
そこで「取引が最も多い上位8件」は新しいペアでも載せるようにした(上の表の3行目)。

1回の追加は `SCOUT_MAX_NEW_POOLS`(既定150)まで。RPC は1回の調査で
`SCOUT_MAX_REQUESTS`(既定40)まで。起動時と6時間ごとに実行する。

環境変数: `SCOUT_CHAINS`(既定 optimism)/ `SCOUT_BLOCKS`(3000)/
`SCOUT_MAX_NEW_POOLS`(150)/ `SCOUT_MAX_REQUESTS`(40)/ `SCOUT_INTERVAL_HOURS`(6)

### 確かめること

- `[プール発見] optimism: …ブロックで○プールが稼働。○件を調べ、**○件**を地図に追加`
  が0件でないこと(0件なら Swap の識別子か読み取りを疑う)
- `[準備完了] optimism` のプール数と、`[惜しい] optimism` の**最小の壁**が下がるか
- `[生存]` の `受信[optimism:N]` と `枠[… 月末見込N%]`。**30%を超えたら**
  `SCOUT_MAX_NEW_POOLS` を下げる
- `[プール発見] … 未採用で取引の多いペア上位:` に出る通貨。ここが次に広げる候補

## 「手数料0bps」という測り間違い(2026年9月20日 17:22 UTC に本番で判明)

### 何が起きたか
polygon の dystopia プール2件で、実測した手数料が **0bps** として採用されていた。

- `dystopia:0xCE1923D2…` … 判定は黒字、チェーン上の確認では **−79.5bps / −204.0bps**。
  誤差は投入額(1,150,000 → 1,495,000)を変えても **−204.0bps のまま一定**。
- `dystopia:0x407ad771…` … 同じく2回続けて赤字。
- `0x5d577d6CdC82d7b6cAC7A101766b68f45bc3e34E` … 送信前の確認で3回続けて
  `execution reverted: "DystPair: K"`(K検算で拒否)。自動的に無効化された。
  生存ログの `失敗段階[feeMismatch:2]` はこれ。

**お金は減っていない。** K検算で弾かれたのは送信前の `eth_call` の段階なので、
ガス代は1円も使っていない。失ったのは機会と、そのプール自身である。

### 原因
手数料の実測は2通りあり、どちらも **x·y=k(定数積)を前提に逆算**している。

1. `getAmountOut` に極小額を聞き、定数積の理論値との差を手数料とみなす
2. 過去の Swap 記録から、定数積の式に必要だった投入量との差を手数料とみなす

Solidly系の **stable プール(x³y+y³x 曲線)** や課税トークンのプールでは、
この前提が成り立たない。差が消える、あるいは逆向きになり、**0bps** が
「測れた値」として採用される。0%のAMMは現実には存在しないので、
**0bps は手数料ではなく「測り方が通用しなかった」印**と読むべきだった。

一度0bpsが採用されると、判定は0bps前提で黒字と見なし → チェーン上の確認で
赤字 → 2回で手数料の印を外して測り直し → **同じ測り方なのでまた0bps** という
輪から抜けられない。3回目で無効化されて終わる。

### 直したこと
- `scripts/onchain-reserves.js`: `MIN_PLAUSIBLE_FEE_BPS`(既定1bps)を下回る
  実測値は採用しない。既定値(30bps)のまま安全側に倒し、24時間後に測り直す。
  `getAmountOut` を持つという印(`byAmountOut` / コントラクトの `FLAG_HAS_QUOTE`)
  だけは残すので、**実行時に送る量はプール自身が計算した正確な値**になる。
  集計行に「値が信用できず既定値のまま N件」を追加した。
- `scripts/pool-scout.js`: イベントからのプール発見でも `stable()` を読み、
  stable プールは載せない。ファクトリーからの取り込み(`pool-discovery.js`)は
  以前から除いていたので、そちらに揃えた形。**Optimism の Velodrome も
  同じ系統**なので、主戦場を広げる前に塞いでおく必要があった。
- `scripts/execute-opportunity.js`: 答え合わせの V2 の段に
  `(準備量の古さ ±Nbps / 式 ±Nbps 手数料 Nbps)` を追加した。
  今までは「同じ式・同じ手数料で準備量だけ読み直す」比較だったため、
  差が出ても **準備量が古かったのか、式や手数料が違うのか** が分からなかった。
  地図が持っている準備量でも同じ式で計算し、2つに分解して出す。

### 次にこのログを見る人へ
`[段ごとの答え合わせ]` の V2 の段で、

- **準備量の古さ**が大きい → 相手の通常取引に先を越されている(競争の問題)
- **式**が大きい → 手数料か曲線の形を取り違えている(こちらの問題)

と読む。後者が出たら、そのプールが stable かどうかをまず疑う。

## ガス単価の補正比が、再デプロイのたびに消えていた(2026年9月20日)

### 何が起きていたか
事前判定に使うガス単価には補正比が掛かる(`getEstimatedGasPriceWei`)。
「出す用意のあった単価(baseFee + priority)」より「実際に取られた実効単価」の
方が低いことが多いので、その比を実測から学ぶ仕組みが2026年9月19日に入っている。

ところが **この学習は送信が成功した時にしか進まない**。成功は1日数件しかない。
そして比はメモリ上にしか無いので、**手を入れて再デプロイするたびに 1.0
(=補正なし)へ戻っていた**。今は1時間に何度も再デプロイしているので、
実際には一度も貯まっていなかったことになる。

### いくら損していたか
実測(2026年9月20日 17:22 UTC、polygon):

```
[実行] 確定: 粗利+$0.0201 − ガス$0.0105 = 純利益+$0.0096(見積もりガス$0.0125)
```

見積もりが実際より **19%高い**。同じ30分の下限の内訳は

```
下限の内訳30分[polygon:127件 粗利中央$0.0138 ガス中央$0.0124 最良純利$0.0048]
```

粗利の中央値$0.0138 に対してガスの見積もりが$0.0124。19%(約$0.0024)が
そのままハードルの高さになっていて、**見送った127件の多くがこの差の中にいる**。
最良でも純利$0.0048 で最低$0.005 に届かない、というのがまさにその状態。

### 直したこと
- `scripts/gas-cost.js`: `exportGasPriceRatios()` / `importGasPriceRatios()` を追加。
- `index.js`: `gas-price-ratio.json`(プール地図と同じ場所)に5分ごとに保存し、
  起動時に戻す。6時間より古いものは使わない。戻す時の件数は最大4に抑えるので、
  その後の実測がすぐ効く。

### 安全のための歯止め(元からある仕組みをそのまま使う)
- 比は **0.7〜1.0** に収める。**見積もりを実際より低くする方向(1.0超)には振らない**。
- つまり最悪でも「実態より3割低く見積もる」までで、ガスが急騰した場面で
  大きく赤字を出す作りにはなっていない。

## 【障害】実測のガス代を確かめ直さずに送っていた(2026年9月21日 00:03 UTC、赤字2件)

### 何が起きたか
最低利益を $0.005 → $0.002 に下げた最初の1時間(23:16〜00:14 UTC)で、
**送信が成功したのに赤字**の取引が2件出た。

```
[実行] 2step polygon sync発見→algebra-a(0.30%): 送信します(投入$2.30 粗利$0.0122 ガス$0.0173 …)
[実行] 確定: 粗利+$0.0122 − ガス$0.0188 = 純利益+$-0.0065(見積もりガス$0.0173)
[実行] 2step polygon polyzap→algebra-a(0.30%): 送信します(投入$4.60 粗利$0.0117 ガス$0.0181 …)
[実行] 確定: 粗利+$0.0117 − ガス$0.0181 = 純利益+$-0.0064(見積もりガス$0.0181)
```

**「送信します」の行に、既に 粗利 < ガス と書いてある。** 知っていて送っていた。

### 原因
`executeOpportunity` の下限の確認は、典型的なガス量(そのチェーン・その段数の
中央値)で行う。その後に `eth_estimateGas` でこの経路の実際のガス量が分かり
`gasCostUsd` を置き換えるが、**置き換えた後に下限を確かめ直していなかった**。
algebra 系や sync発見のプールを含む経路は典型値より多くガスを使うため、
確認は通るのに実測では赤字、という隙間ができる。

なぜ今まで表に出なかったか:最低利益 $0.005 の余裕が、典型値と実測値の差を
吸収していた。$0.002 に下げた瞬間に隙間が見えた。閾値の問題ではなく、
確認の順序の問題。

### 直したこと
`scripts/execute-opportunity.js`: 実測のガス代で `gasCostUsd` を置き換えた直後に
`grossProfitUsd - gasCostUsd >= MIN_PROFIT_USD` を確かめ直す。満たさなければ
`below_gas` として見送る(ログ:「実測のガス代…を引くと純利…で下限未満のため見送り」)。

### 同じ1時間の他の数字(参考)
- 成功7件のうち黒字5件(+$0.0057 / +$0.0047 / +$0.0241 / +$0.0037 / +$0.0051)。
  赤字2件を除けば +$0.043。1時間の黒字件数は昨夜の一晩分(3件)を超えた
- 送信後の revert が4件(すべて polygon、sync発見のプールを含む経路)。3回で
  自動無効化が働き 0xE49fea62… / 0xAE81FAc6… を外した。revert の理由は null
- 実測ガス/見積もりの比は polygon で 0.90 / 1.00 / 1.00(補正0.967)。
  見積もり自体は当たっている。当たっているのに送ったのがこの障害

## 「壁が高い経路は無理」は間違いだった(2026年9月21日、実測で訂正)

### 何を間違えていたか
「あと何bpsで黒字だったか」の画面は、壁が `REACHABLE_WALL_BPS`(既定60)以下の
経路だけを惜しさの分布に入れていた。根拠は「0.3%+0.3%=60bps までしか裁定は
成り立たない。1%+1%=200bps は価格がどれだけ動いても黒字にならない」。

**この前提が実測と合わない。** 実際に黒字になった取引の壁を数えると、
8件中5件が60bps以上だった。

| 壁 | 経路 | 純利益 |
|---|---|---|
| 105bps | avalanche pangolin→uniswap-v3(0.30%)→pangolin | +$0.0093 |
| 101bps | arbitrum uniswap-v3(1.00%)→uniswap-v3(0.01%) | +$0.0097 |
| 75bps | polygon algebra-a(0.30%)→dfyn→dystopia | +$0.0087 |
| 65bps | polygon uniswap-v3(0.05%)→dfyn→sync発見 | +$0.0057 |
| 60bps | avalanche pangolin→univ3-fork-f(0.30%) | +$0.0077 |

理由は単純で、**浅いプールでは価格差が100〜160bpsに達する**から。同じ時間帯の
粗利は +48.9 / +62.9 / +63.5 / +75.0 / +88.0 / +138.2 / +153.7 / +161.5 bps。
90bpsの壁は160bpsの価格差の前では障害にならない。**1%のプールでも黒字になる。**

### いくら見落としていたか
polygon の壁の高さの内訳(2026年9月21日 10:06 JST の画面):

| 壁 | 本数 | 割合 |
|---|---|---|
| 20bps以下 | 5 | 0.2% |
| 20〜40bps | 114 | 3.5% |
| 40〜60bps | 91 | 2.8% |
| **60〜100bps** | **2,791** | **85.2%** |
| 100〜200bps | 273 | 8.3% |

60bps で切っていたため、**3,274本中210本(6.4%)しか惜しさの分布に入らず、
実際に稼いでいる85%の帯が丸ごと視界の外**にあった。しかもその210本のうち
181本は −100bps未満の絶望的な経路で、生きているのは29本だけに見えていた。

### 直したこと
- `scripts/opportunity-scanner.js`: `REACHABLE_WALL_BPS` の既定を 60 → **100**。
  **表示にしか使っていない**(getNearMissStats の絞り込み)ので、判定にも送信にも
  影響しない。100〜200bps でも黒字は出る(上の arbitrum 101bps)ので、そちらは
  壁の高さの内訳で別途見る
- `index.js`: 画面の説明文を訂正。「手数料1%のプールを2段通れば…黒字になりません」
  という誤った断定を削り、「壁が高い=無理、ではない」と実測値を書いた

### 覚えておくこと
**「壁」は障害の大きさではなく、必要な価格差の大きさ**でしかない。
深いプール(壁3〜6bps)は価格差がほぼ0で取れない。浅いプール(壁60〜100bps)は
価格差が100〜160bpsあるので取れる。**この2つを同じ物差しで見てはいけない。**

## 価格表のずれを測るだけで直していなかった(2026年9月21日)

### 何が起きたか
arbitrum の 1.00% のプールを1段目に使う経路が、3本続けて幻の機会になった。

```
00:46 [機会] uniswap-v3(1.00%)→uniswap-v3(0.01%): 純利益+$0.0164(投入$89.70)
      [実行] チェーン上の計算では赤字(-1.2bps)
      [段ごとの答え合わせ] 1段目 uniswap-v3(v3) -4.0bps(公式-4.0bps)
00:47 [機会] uniswap-v3(1.00%)→uniswap-v3(0.05%): 純利益+$0.0417(投入$96.60)
      [実行] チェーン上の計算では赤字(-4.3bps)
      [段ごとの答え合わせ] 1段目 -9.0bps(公式-9.0bps) ← 1段目 uniswap-v3:0x42FC852A… が原因
00:48 [機会] uniswap-v3(1.00%)→uniswap-v3(0.05%): 純利益+$0.0126(投入$89.70)
      [実行] チェーン上の計算では赤字(-7.8bps)
```

**公式Quoterと自前の見積もりが一致している**(`公式-9.0bps`)ので、
コントラクトや手数料帯の取り違えではない。**価格表(quote table)そのものが
外れている**。

### 原因
`verifyV3Calculations` は同じ時間帯にこう言っていた。

```
[V3検証] avalanche 0xd18384F4…(1.00%) 投入$200: 補間が公式より過小28.5bps
[V3検証] avalanche 0xd18384F4…(1.00%) 投入$700: 補間が公式より過小92.7bps
```

手数料1%の帯は tick の刻みが広く、流動性が塊で置かれているため、表の点と点の
間を直線で結ぶと大きく外れる。しかも**投入額が大きいほど外れる**
($200で28.5bps → $700で92.7bps)。

そして決定的なのは、**この検証は測って出すだけで、結果を一切使っていなかった**
こと。92.7bps ずれていると分かっている表を、そのまま判定に使い続けていた。

**狙いたい大口の帯(投入$90〜700)が、まさにこの誤差で幻になっていた。**

### 直したこと
`index.js`: ずれが `VERIFY_DROP_TABLE_BPS`(既定50bps)を超えたら、その
プールの価格表を捨てる(`clearQuoteTable`)。次に必要になった時に
「要求で作成」の仕組みが作り直す。捨てても損はしない。
生存ログ用に `stats.v3VerifyDropped` を追加。

閾値を20bps(ログに出す閾値)ではなく50bpsにしたのは、20bpsで捨てると
作り直しが増えすぎるため。狙う利幅の上限を超えたら使えない、という線。

### 覚えておくこと
**測っているのに使っていない値が他にもないか**を疑うこと。この節の1つ前の
「ガス単価の補正比が再デプロイのたびに消えていた」も同じ型の見落としだった。

## 反応の遅れを削る 第1段(2026年9月21日)

### なぜやるか
実測のループは **600〜800ms**。内訳は Flashblocks の取得200ms(400msごとの
取りに行き)+ 確認85〜170ms + 準備177〜620ms + Railway(sfo)→ 米東海岸の
シーケンサー60〜80ms。Flashblock は200msごとに配られるので、**3〜4個ぶん
遅れて**撃っていた。先読みで1秒早く見えている優位を、自分で食い潰していた。

### やったこと(この回)
**① 確認とガス量の見積もりを同時に投げる(−85〜170ms)**

確認(`eth_call`)とガス量の見積もり(`eth_estimateGas`)は、どちらも
「今の状態でこの取引を実行したらどうなるか」を聞いている。順番に待っていた
理由は「見積もりに minProfit が要る」からだけだった。

その minProfit は**ガス量をほとんど変えない**(受取との比較が1回増えるだけ)。
そこで見積もりは `minProfit = 0` で先に投げる。実際に送る時は、今までどおり
確認の結果から作った本物の minProfit を渡す。**送信の可否の判断は一切変えて
いない。**

**② L1 データ手数料を1件ごとに聞くのをやめた(−約100ms、OP Stackのみ)**

送信のたびに GasPriceOracle に聞いていたが、**1往復かけて測っている額が
$0.000003**(2026年9月20日の実測)。Optimism のガス代$0.005 に対して 0.06%。
代表値(受領証から学んだ実測の平均。`recordActualL1Fee` が更新)で十分。

### まだやっていないこと(次の回)
- Flashblocks を400msごとの取りに行きから WebSocket の押し出しに(−100〜200ms)
- シーケンサーの受付URLへ直接送る(−20〜50ms)
- Railway を US East へ(−50〜60ms。ボリュームの引っ越しを伴う。オーナー作業)

### 測り方
`[実行] … 送信します(… 確認Nms 準備Nms)` の2つの数字を見る。
変更前は 確認85〜170ms / 準備118〜620ms。**確認の数字は同時に投げた分だけ
大きく見える**(ガス見積もりの待ちも含むため)が、**準備との合計が縮む**。

## 反応の遅れを削る 第2段: 確定前のイベントを押し出しで受け取る(2026年9月21日)

### なぜやるか
今は 400ms ごとに `eth_getLogs(pending)` を取りに行っている。Flashblock は
**136〜234ms ごと**に配られるので(下の実測)、平均200msの取りこぼし遅れが
必ず乗る。しかも1日216,000回・月650万回(枠2,000万の**3割**)をこれだけで
使っている。押し出しで受け取れれば、**遅れと RPC の消費が同時に消える**。

### やったこと
`eth_subscribe` で確定前のイベントの購読を試す(`PENDING_SUB_METHODS`、
既定は `pendingLogs,preconfLogs` の順)。通れば定期取得を止め、切断されたら
自動で取得に戻る。**端点が対応していなければ拒否されるだけで、今までどおり
取りに行く**(安全側)。

生存ログの先読みの欄が `取得N回` から `押し出しN回` に変わる。

### 【障害】ping の返事を購読成功と誤認した(2026年9月21日 02:17 UTC、即日修正)
押し出しの購読の返事を `msg.id >= 500` で拾っていたが、生存確認の ping は
`PING_REQUEST_ID = 999` を使っている。**ping の返事が「500以上」に当てはまり、
購読が成功したと誤認して定期取得を止めた。**

```
[Flashblocks/押し出し] optimism: 確定前のイベントを押し出しで受け取ります。定期取得は止めました
[Flashblocks/押し出し] arbitrum: 〃
[Flashblocks/押し出し] avalanche: 〃
[Flashblocks/押し出し] base: 〃
[Flashblocks/押し出し] polygon: 〃
```

**polygon / arbitrum / avalanche は Flashblocks の対象外で購読要求すら送って
いない**のに成功と出たのが決定的な印。この間、先読みは全チェーンで止まっていた
(購読IDの代わりにブロック番号が入るため、実際のイベントとは一生照合しない)。

直し方:
- 範囲で判定する(2000〜2999)。ping の 999 より大きくし、上限も決める
- 購読IDは「0x + 32桁程度の十分に長い文字列」であることを確かめる
- 確定後の購読の判定にも `msg.id < PING_REQUEST_ID` を足した

**教訓:番号の範囲で種類を見分けるなら、必ず上限も決めること。**

### 見張りも足した(同日)
購読が**受理されても何も配信されない**端点があり得る。その場合、定期取得は
止まっているので**先読みが黙って死ぬ**。先読みはこの bot の主な強みなので、
黙って失うのが最悪。

`PENDING_PUSH_SILENCE_MS`(既定90秒)の間、押し出しでイベントが1件も来ず、
かつ**確定後のイベントは届いている**なら、配信されていないと見なして定期取得に
戻す(以降その端点では押し出しを試さない)。

### Base では newFlashblocks が通る(2026年9月21日に判明)
Base を稼働に加えた初回の起動でこう出た。

```
[Flashblocks] base: 対応あり(newFlashblocks を購読できました)。配信間隔 138/136/160/173/234ms
```

**Optimism では Chainstack に拒否されていた `newFlashblocks` が、Base では通る。**
上の `pendingLogs` が Base で通らなかった場合は、`newFlashblocks` の配信
(部分ブロックそのもの)から受領証のイベントを取り出す実装に進む。

## オーナーの判断が要る時だけ LINE に通知する(2026年9月21日)

### 前提
- **LINE Notify は2025年3月31日に終了**した。代わりに Messaging API の
  プッシュメッセージを使う
- 無料枠は**月200通**(宛先数 × 通数)
- Claude の作業環境からは `api.line.me` に届かない(外向き通信が遮断されている)。
  **bot 自身(Railway)から送る**

### 方針:通知するのは「オーナーが動かないと解決しないこと」だけ
普段の失敗・機会ゼロ・静かな市況は通知しない。こちらで対処できるため。
通知が多いと読まれなくなり、**本当に必要な1通が埋もれる**。

| 送る | なぜ |
|---|---|
| 全チェーンが10分以上止まった | 機会をすべて失っている |
| RPCの枠の月末見込が70%超 | 使い切ると全部止まる。プランか監視範囲の判断が要る |
| 送信用ウォレットのガス残高が下限割れ | **補充はオーナーにしかできない** |
| 送信後の失敗が5件積み上がった | ガス代だけを失っている可能性 |

| 送らない | なぜ |
|---|---|
| 通常の失敗・機会ゼロ・閑散 | 正常な範囲 |
| こちらのコード修正 | 事後に報告すれば足りる |

### 実装
- `scripts/owner-alert.js`: `alertOwner(key, title, body)`。用件ごとに冷却
  (既定6時間)、1日の上限(既定10通)。**未設定なら何もしない**(ログにだけ出す)
  ので、設定前でも安全に動く
- `index.js`: 生存ログのたびに `checkOwnerAlerts()` が条件を確かめる。
  ガス残高だけは30分に1回、チェーンごとに1回の呼び出しで読む

### 必要な環境変数(Railway)
| 変数名 | 取り方 |
|---|---|
| `LINE_CHANNEL_ACCESS_TOKEN` | LINE Developers → チャネル → Messaging API タブ → チャネルアクセストークン(長期) |
| `LINE_USER_ID` | 同 → Basic settings タブ → Your user ID |

調整用: `ALERT_ENABLED`(false で停止) / `ALERT_COOLDOWN_MS` /
`ALERT_MAX_PER_DAY` / `ALERT_QUOTA_PERCENT` / `ALERT_MIN_GAS_NATIVE` /
`ALERT_ALL_DOWN_MS` / `ALERT_CONSECUTIVE_SEND_FAILS`

### Claude からの質問も LINE へ転送する(同日に追加)

**なぜこの形なのか。** Claude の作業環境からは `api.line.me` に届かない。
Railway のアプリ用ドメイン(`*.up.railway.app`)にも届かないので、HTTPで直接
渡す経路も無い(プロキシの許可リストに無い。`$HTTPS_PROXY/__agentproxy/status`
で確認済み)。そこで**リポジトリをメールボックスにする**。

**使い方(Claude 向け)。** `docs/owner-questions.json` の `questions` に足して
コミットする。そのデプロイでファイルごと bot に届き、bot が LINE へ転送する。

```json
{ "questions": [
  { "id": "2026-09-21-01", "title": "元手の投入",
    "body": "2チェーン間の裁定に $2,000 × 2 を入れますか。ETH の価格リスクを取ることになります。" }
] }
```

- **id は二度と重複しない値にする**(日付+連番など)
- **片付いたら `"resolved": true` と `"resolvedNote"` を付ける。**
  消さずに残すので経緯は追えるが、印が無いと**解決済みの依頼が送られ続ける**
  (下の不具合を参照)
- 送信済みの id はボリューム(`owner-questions-sent.json`)に記録し、
  **再デプロイしても二度と送らない**。無料枠を守り、何より読まれなくなるのを防ぐ
- 解決した質問は**消さずに残す**。id が同じなら再送されないので、経緯が残る
- 送れなかった分は10分ごとに再試行する
- LINE 未設定の間は送らず、ログ(`[要判断]`)にだけ出る

**この経路の代償は「デプロイが1回要る」こと。** 質問はコードの修正と一緒に
出ることが多いので、実際には余分なデプロイにならないことが多い。

## Base の初日に、今までの50倍の機会が出た(2026年9月21日 02:49 UTC)

Base を稼働に加えた最初の数分で、これが出た。

```
[機会] 3step base uniswap-v3(0.05%)→aerodrome→sync発見: 純利益+$0.4875(投入$30.00 壁0.55%)
[機会] 3step base uniswap-v3(0.30%)→aerodrome→sync発見: 純利益+$0.4516(投入$30.00 壁0.80%)
[機会] 3step base uniswap-v3(1.00%)→aerodrome→sync発見: 純利益+$0.3640(投入$24.15 壁1.50%)
```

**1回$0.36〜$0.49。** 今までの1回$0.005〜0.024 の**20〜50倍**。しかも壁は
0.55〜1.50% と高く、「壁が高い=無理ではない」という9月21日の訂正の裏づけでもある。
Aerodrome は Base の出来高の50〜63%を握る DEX で、そこに絡む経路である点も
筋が通っている。

**ただし3件とも確認(eth_call)で `missing revert data` になり、送れていない。**

### 原因を推測で決めない
考えられるのは2つ。

1. **Base のコントラクトが旧版**(`[コントラクト] base 0x68bCbb3f…: 旧版(tokenIn/kind)`)。
   他の4チェーンはガス削減版に再デプロイ済みだが、Base だけ取り残されている
2. **pending ブロックでは revert の中身が返らない端点**。Base はこの日から
   `FLASHBLOCKS_PENDING_CHAINS` に入れて pending で読むようにしたばかり

この確認は**わざと revert させて結果を受け取る**作りなので、revert の中身が
落ちると何も分からない。旧版でも `simulateRoute` と `SimulationResult` は
同じ署名で存在する(git で確認済み)ので、1 と断定はできない。

### 切り分けの仕掛けを入れた
`scripts/execute-opportunity.js`: pending で `missing revert data` になった時
**だけ** latest で聞き直す。返ればそのチェーンは以降 latest を使い、ログに
残す。判定に使った状態とは少しずれるが、確認できない方が悪い。

- latest で通る → 原因は 2(端点の挙動)。再デプロイは要らない
- latest でも同じ → 原因は 1(旧版)。**Base の再デプロイをオーナーに依頼する**

### 機会を待たずに起動時に確かめる(同日に追加)
上の切り分けは「次の機会が来るまで答えが出ない」。Base の大きな機会は
数分〜数十分に1回しか出ないので、偶然待ちでは遅い。

**決定的な試験がある。** `simulateRoute` には `onlyOwner` が付いていて、
所有者以外が呼べば必ず `require(msg.sender == owner, "DexArbFlashLoan: not owner")`
で revert する。**状態に一切依存せず、決まった答えが返る。**

`probeRevertData()`: 所有者ではない住所(0x…01)から `simulateRoute` を
`pending` で呼び、revert の中身が返るかを見る。返らなければ `latest` でも試す。
`latest` で返れば、そのチェーンは以降 `latest` で確認する。
起動時の `checkContractVersions` から、pending を使うチェーンだけ呼ぶ
(1チェーンにつき最大2回の呼び出し)。

### 答え(2026年9月21日 03:12 UTC、起動時の試験で確定)

**対照実験がはっきり出た。**

| チェーン | コントラクト | 確認の下調べ |
|---|---|---|
| optimism | ガス削減版 | **pending でも revert の中身が返ります** |
| **base** | **旧版** | **pending でも latest でも返りません** |

**端点の問題ではない。** Optimism は同じ仕組み・同じブロック指定で合格している。
**Base のコントラクトが、bot の期待する形の応答を返していない。**

`eth_call(pending)` 自体は Base でも通っている(Flashblocks の下調べで確認済み)
ので、呼び出しの経路は生きている。落ちているのは **revert の中身**だけ。

### 試験をもう一段細かくした(同日)
「取り消されなかった」と「取り消されたが中身が無い」は原因が全く違う。

- **取り消されない** → その住所に中身が無いか、別のコントラクト
- **中身が無い** → 端点が落としているか、理由なしで取り消している

分けて報せないと次に何をすればいいか決められないので、`probe()` が
3通り(`returned` / `revertWithData` / `revertNoData`)を返すようにし、
revert の理由の文字列も取り出してログに出す。

### 結論:**Base の再デプロイが要る** → 同日 03:17 UTC に実施、解決

オーナーの了承を得て再デプロイした。

```
[本番デプロイ] base へのデプロイを開始します
[本番デプロイ] ウォレット 0x9D9263… の残高: 0.002911839741691365
[本番デプロイ] base 完了: 0xD2D45cC99AAe1AF7302b067d116fEEA8d7ceAca1
[コントラクト] base 0xD2D45cC9…: ガス削減版(flags) と判別しました
[確認の下調べ] base: pending でも revert の中身が返ります(「DexArbFlashLoan: not owner」)。このままで大丈夫
```

**直前まで不合格だった試験に合格した。** revert の理由の文字列まで読めている。
これで Base の機会も他の4チェーンと同じように確認できる。

**新しい住所が arbitrum と同じに見えるが、これは正常。** 住所は「送信者 + nonce」
から決まるので、同じウォレットが同じ nonce で同じコードをデプロイすれば、
チェーンが違っても一致する。Base 上で新しく作られたことは上の判別ログが証明している。

#### 2026年9月21日時点の全チェーンのコントラクト

| チェーン | 住所 | 版 |
|---|---|---|
| polygon | `0xd0Fae44b77833744faFfA012082ba001DfB7E0fF` | ガス削減版 |
| arbitrum | `0xD2D45cC99AAe1AF7302b067d116fEEA8d7ceAca1` | ガス削減版 |
| avalanche | `0x3bCdCC60b0d1A8a98152A08985dF87C47f2e4Cc9` | ガス削減版 |
| optimism | `0x43e0536aA8F740253A7f6580565695c7F7A3E9A7` | ガス削減版 |
| **base** | `0xD2D45cC99AAe1AF7302b067d116fEEA8d7ceAca1` | **ガス削減版(9/21に再デプロイ)** |

**旧 Base コントラクト `0x68bCbb3f8ec1E783E176Ca76b0ecDc758fdf9B6F` は残したまま。**
所有者は変わらない(immutable)ので、中に資産があれば後からいつでも withdraw できる。
Base は長く停止していて取引実績が無いため、急がなかった。

#### 再デプロイの手順(次に別のチェーンで行う時のために)
1. `RUN_MAINNET_DEPLOY=<チェーン名>` を設定(再デプロイが走り、起動の早い段階で
   コントラクトが展開される。その後 bot は通常どおり動き続ける)
2. ログの `[本番デプロイ] <チェーン> 完了: 0x…` から住所を読む
3. `MAINNET_CONTRACT_ADDRESS_<CHAIN>` に住所を設定し、同時に
   `RUN_MAINNET_DEPLOY=false` に戻す(1回の設定変更でまとめる)
4. `[コントラクト] … ガス削減版(flags) と判別しました` と
   `[確認の下調べ] … このままで大丈夫` の2行で成功を確かめる

判定は狭くする(`missing revert data` の文字だけ)。本物の revert で聞き直すと
無駄な問い合わせが増えるだけで何も分からない。

## 【誤報】ガス残高の下限を通貨の量で決めていた(2026年9月21日、即日修正)

通知を入れた初日に、さっそく誤報を出した。

```
[要判断] arbitrum のガス残高が足りません / 残高が 0.00170 です(下限 0.002)
```

下限を「0.002(そのチェーンの通貨)」で決めていたが、これは雑すぎた。
**0.002 ETH は約$5(arbitrum で約340回ぶん)なのに、0.002 MATIC は約$0.001
(polygon で0回ぶん)。** 同じ数字が意味する余裕が桁違いに違う。

直し方:**「あと何回送れるか」で決める**(既定100回)。残高と1回あたりの
ガス代をどちらもUSDに直して割るだけで、通貨にもガス相場にも左右されない。
環境変数は `ALERT_MIN_REMAINING_SENDS`。

**教訓:閾値は「単位のある量」ではなく「意味のある量」で決めること。**
誤報は1通でも通知の信用を落とす。

## 【不具合】片付いた質問が送られ続けていた(2026年9月21日、即日修正)

質問の転送を入れた直後に、Base の再デプロイの依頼を書いた。ところがその依頼は
**オーナーがすぐ実行してくれて解決した**のに、LINE が未設定で「未送信」のまま
だったため、**10分ごとに再試行され続けた**。

```
[要判断] 判断をお願いします: Base のコントラクト再デプロイ(立ち会いのお願い)
  +$0.4875(投入$30) / +$0.4516(投入$30) / +$0.3640(投入$24)
  …
```

このまま夜に LINE を設定すれば、**済んだことの依頼がいきなり届く。**
誤報は1通でも通知の信用を落とす(ガス残高の下限に続いて2件目の誤報)。

### 直したこと
- `"resolved": true` が付いた質問は送らない。質問は経緯を残すために消さないので、
  **片付いた印を付けて飛ばす**
- 未解決の質問のログは**この起動中に1回だけ**にする(`quiet` 指定)。
  10分ごとに同じ文面が流れると、他のログが読めなくなる
- `alertOwner(key, title, body, { quiet })` に拡張。既定は今までどおり毎回出す

### 覚えておくこと
**「送るかどうか」だけでなく「もう送る必要が無いか」も考えること。**
再試行する仕組みを作ったら、必ず**止め方**も一緒に作る。

## Aave 清算 第1段の初回測定と、そこで見つけた設計の欠陥(2026年9月21日)

`scripts/aave-liquidation.js`(読み取りのみ)を入れた直後の16分ぶんの実測。

### 動いたこと

```
[清算] base: Aave V3 Pool 0xA238Dd80C259a72e81d7e4664a9801593F98d1c5 に応答を確認しました
[清算] optimism: Aave V3 Pool 0x794a61358D6845594F94dc1DB02A252b5b4814aD に応答を確認しました
```

設計書で「要確認」としていた optimism の Pool 住所が、**実測で応答を返した**。

| | base | optimism |
|---|---|---|
| 借り手(名簿) | 168 → 223人 | 3 → 6人 |
| 危険(HF<1.05) | 4人 | 0人 |
| 清算可(HF<1.00) | 0件 | 0件 |

RPC は 2→39回。枠は 6.51%→6.56%、月末見込は 23% のまま。裁定側への影響なし。

### 見つけた欠陥:**名簿が完成しない**

ログの「過去へ ◯◯ まで遡り済み」を2回ぶん引き算すると、**10分で4,000ブロック**しか
遡れていなかった(2,000ブロック固定 × 2本)。

| | 遡る必要 | この速さでの所要 |
|---|---|---|
| base | 約4,920万ブロック | **約85日** |
| optimism | 約1億5,280万ブロック | **約265日** |

**「1〜3日測って第2段に進むか決める」が成立しない。**
名簿は直近2〜3日ぶんの借り手しか載らないので、「清算可0件」が本当に0件なのか、
名簿に載っていないだけなのかを区別できなかった。

### 対策①:読み取り幅を実測で合わせる

2,000ブロック固定をやめ、**成功したら倍・断られたら半分**にした(500〜50,000)。
呼び出し回数は増えず、1回で読む幅だけが広がる。

- 幅の育ち方: 2,000 → 4,000 → 8,000 → 16,000 → 32,000 → 50,000(頭打ち)
- 10分あたり 200,000ブロック、1日あたり 2,880万ブロック
- **base は 85日 → 1.7日**

**「今の幅いっぱいを読めた時だけ広げる」**ようにしてある。追いついた後の
数百ブロックの成功を「余裕がある証拠」にすると、実力より広く育って無駄に断られる。

「幅が広すぎる」と断られた回数(`shrinks`)は、通信の失敗(`errors`)とは
**別に数える**。想定内の調整を失敗として出すと、本当の障害が埋もれる。

### 対策②:名簿に頼らず、**実際に起きた清算**を数える ← 本命

`LiquidationCall` イベントを遡って読む。清算は滅多に起きないので、1回の読み取りで
何十万ブロックも見られる。名簿の完成を待たずに第1段の問いへ直接答えが出る。

```
[清算/実績] base: 約N日ぶんで M件(1日あたりX件)。新規K件。RPC N回
  規模: $100未満 a件(p%) / $100〜1,000 b件 / $1,000超 c件、中央$Z
  清算した人: K人、上位1者がQ%(0x…… N件)。※金額は現在の価格での換算
```

**「小口は放置されている」は、外部の研究からの推定でしかなかった。**
ここを実績の分布で置き換える。「誰が取っているか」まで数えるのは、裁定で
「他者の裁定0件」という決定的な事実を掴めたのと同じ狙い。

- topic0 `0xe413a321…` は `ethers.id()` で計算して確かめた(思い込みで書かない)
- 添字なしの4語(debtToCover / 受け取った担保 / 清算した人 / aToken受取)の
  切り出しは、`ethers` の正式なエンコーダで作ったログと突き合わせて一致を確認した
- 金額は **Aave 自身の価格オラクル**から取る(`Pool.ADDRESSES_PROVIDER()` →
  `getPriceOracle()` → `getAssetPrice()`)。清算の判定に使われているのと同じ価格
- **現在の価格での換算**である点は正直にログに書く。ステーブルの借金なら正確だが、
  変動資産の借金は目安にしかならない

### 教訓

**「少しずつ進む」作りを入れたら、必ず『いつ終わるか』を数字で出すこと。**
毎回きちんと前進していたので動作は正常に見えたが、**終わるのが85日後**だった。
進捗のログは出していたのに、私はその差分を割り算していなかった。

もうひとつ。**測る手段は、答えたい問いから逆算すること。** 名簿から
「これから清算されそうな人」を探すのは第2段(実際に取る)には要るが、
第1段の問い(規模はいくらか・誰が取っているか)には実績を数える方が
けた違いに速く、しかも正確だった。

---

## Aave 清算の実績、最初の数字(2026年9月21日 04:27 UTC)

読み取り幅の自動調整を入れた直後、**わずか1分**で過去0.5日ぶんの実績が出た。
名簿を85日待つ必要はなくなった。

| | base | optimism |
|---|---|---|
| 清算の件数(0.5日) | 9件(1日あたり17.7件) | 14件(1日あたり27.5件) |
| $100未満 | 6件(67%) | **14件(100%)** |
| $1,000超 | 3件 | 0件 |
| 中央値 | $40.97 | **$0.00** |
| 清算した人 | **6人、上位1者22%** | **1人、上位1者100%**(0x61e1B297…) |
| 価格オラクル | `0x2Cc0Fc26eD4563A5ce5e8bdcfe1A2878676Ae156` | `0xD81eb3728a631871a7eBBaD631b5f424909f0c77` |

名簿も一気に伸びた: base 279→536人、optimism 6→23人。幅は16,000まで育った。

### いちばん大事な発見(暫定): **base は分散している**

base は **6人が9件**を分け合い、上位1者でも22%。
一方 optimism は **1人が14件すべて**を取っている。

「清算の46%を上位5者が握る(Gini 0.83)」という外部の研究は**大口の話**であり、
base の小口帯では当てはまらない可能性がある。これは裁定で見つけた
「浅い帯は他者の裁定0件」と**同じ構造**かもしれない。

ただし **0.5日・23件の標本**でしかない。結論を出すには早い。

### そして、信用してはいけない数字

optimism の「$100未満 14件(100%) 中央$0.00」。
**本当にダストばかりなのか、桁数か単価の読み違いで0に潰れているのかを、
合計した数字からは区別できない。**

この「小口N%」は**第2段に進むかを決める数字そのもの**なので、疑わしいものを
小口に混ぜてはいけない。手数料が0bpsに見えた時(2026年9月20日)と同じ間違い。

対策(即日):
- **$0.01未満は「ごく少額」として小口と分けて数える**
- **生の値をログに出す**(桁数・単価・生の量・換算結果)。潰れているならそこに出る

```
[清算/実績/内訳] optimism ブロック157,160,000: USDC(6桁 単価$1.0001) 生の量 1234 → $0.001234
規模: ごく少額(<$0.01) t件 / $0.01〜100 a件(p%) / $100〜1,000 b件 / $1,000超 c件
```

> **教訓:合計した数字が怪しい時は、何からできているかを見られるようにしておくこと。**
> 「$0.00」は答えではなく、**問いが立った合図**。

---

## 清算を「件」で数えていたのは間違いだった(2026年9月21日、生の値で判明)

前の節で「optimism の中央$0.00 は読み違いかもしれない」と書き、生の値を出すようにした。
出てきた答えは **読み違いではなかった**。

```
[清算/実績/内訳] optimism ブロック157,152,956: USDC(6桁 単価$1.0000) 生の量 1302 → $0.001302
[清算/実績/内訳] base     ブロック51,531,943: WETH(18桁 単価$2661.7460) 生の量 9527 → $0.000000
[清算/実績/内訳] optimism ブロック157,124,283: USDT(6桁 単価$0.9997) 生の量 186 → $0.000186
```

桁数も単価も正しく、本当に 0.0013 USDC・9527 wei という塵だった。

### では何が問題だったか

**`LiquidationCall` は (担保, 借金) の組ごとに出る。**
大きな清算を1回やると、端数の残りでもう1件、塵のような記録が出ることがある。
1件ずつ数えると、**「大きな清算のおまけ」を「小口の機会」として数えてしまう**。

> **数えるべき単位は『清算1回』であって『イベント1件』ではない。**

対策: **同じ人・同じブロックを1回にまとめる**(`${user}:${block}`)。
金額は足し合わせ、清算した人もその回の人として数える。

検算(同じ手順で計算した):

```
生の記録 4件 → 清算 3回
規模: ごく少額 1回 / $0.01〜100 1回 / $1,000超 1回
旧: ごく少額 2件(50%) ← 大きな清算の端数を小口として数えていた
```

数え方を変えたので、保存済みの集計には版(`HIST_VERSION`)を付けて作り直す。
**作り直す時は読み取りの進み具合も戻す。** 戻さないと、過去を読み直せないまま
空の集計だけが残る(「再試行する仕組みを作ったら止め方も作る」の裏返し)。

### この時点で分かっている base の実測(2日ぶん)

| | base | optimism |
|---|---|---|
| 生の記録 | 11件 | 16件 |
| **$1,000超** | **4件** | 0件 |
| $0.01〜100 | 6件 | 0件 |
| ごく少額 | 1件 | 16件(全部) |
| 中央値 | $40.97 | $0.00 |

**base には1日あたり2件前後、$1,000を超える清算がある。**
50%まで肩代わりしてボーナス5%なら、1回の粗利は **$25以上**。

---

## 大きな機会が消えていたのに、数えていなかった(2026年9月21日、オーナーの指摘)

オーナーの指摘:

> 動き始めた初日は $0.3 ぐらいの利益が3〜4回見えていた。
> その後しばらく利益が取れなくなり、自動売買が成功するようになったら
> 桁の小さい利益しか出なくなった。

**調べたところ、確かめる手段が無かった。** これが本題。

### handleOpportunity には機会を捨てる道が7つあり、4つは無記録だった

| 捨てる道 | 記録 |
|---|---|
| `hasDisabledPool` | **無し** |
| 冷却中 | **無し** |
| 同じ経路を送信中 | **無し** |
| 同じプールが使用中 / 同時送信の上限 | **無し** |
| 税トークン | あり |
| 罠 | あり |
| 下限を下回った | あり |

そして index.js の注釈には、こう書いてあった。

> 「粗利$0.4155の機会を丸ごと失っている」

**大きな機会が消えることは分かっていたのに、何件消えたかを数えていなかった。**
数えていない以上、「初日より悪くなった」も「気のせい」も、どちらも証明できない。

### 対策: 大物の台帳(`scripts/big-opportunities.js`)

$0.10以上(今の平均$0.005の**20倍以上**)の機会を、**捨てる道すべてで1件ずつ記録する**。

```
[大物/逃した] base 2step …: 見込み$0.4875(投入$30.00) → **同じプールが使用中**(同時3本)
[大物] 30分: $0.10以上を 7件検知 → 成立1件(実際に$0.31) / 逃した6件(見込み$1.84)[冷却3 送信中2 罠1]
```

生存ログにも1行: `大物[検知N 成立M 得た$X 逃した見込み$Y]`

**RPCは一切使わない**(すでに手元にある値を数えるだけ)。

> **教訓:捨てる場所を作ったら、捨てた物を数えること。**
> 「機会が無い」と「機会を捨てていた」は、数えないと区別できない。

---

## 【不具合】取引上限が$500から一度も上がっていなかった(2026年9月21日)

`scripts/trade-cap.js` の成功回数の保存先が **`/tmp`** だった。
**/tmp は再デプロイのたびに消える。**

```
const CAP_STAGES = [
  { minSuccesses: 0, maxTradeUsd: 500 },
  { minSuccesses: 3, maxTradeUsd: 1000 },   ← 到達不能
  { minSuccesses: 8, maxTradeUsd: 2000 },   ← 到達不能
];
```

3回成功すれば$1,000に上がる仕組みを作っておきながら、**その条件が永久に
満たされない**状態だった。今日だけで7回デプロイしているので、なおさら。

ガス単価の補正比が**まったく同じ理由**で消えていたのを9月20日に直したのに、
**同じ間違いがここに残っていた**。プール地図と同じ場所(ボリューム)へ移した。

> **教訓:同じ種類の不具合を1つ直したら、同じ形の場所を全部探すこと。**
> `/tmp` を既定にしている保存先が他に無いかを、その場で確かめるべきだった。

**ただし今の取引額は$1〜30なので、$500の上限は効いていない。**
効き始めるのは、深いプールで大きな機会を取れるようになってから。

### 同じ形の場所を全部探した(その場で実行した)

`/tmp` を既定にしていた保存先を洗い出し、**4つ**見つけて揃えた。

| ファイル | 何を失っていたか |
|---|---|
| `verified-pairs.js` | 実行可能と確認済みのペア一覧と、**実測した手数料** |
| `incompatible-pools.js` | 不適合と分かったプール。**毎回ガスを払って学び直していた** |
| `opportunity-journal.js` | 機会の記録簿 |
| `real-execution-log.js` | 実際の送信の記録 |

今日だけで7回デプロイしているので、**学んだことを7回捨てていた**。
とくに `incompatible-pools` は、同じ失敗を繰り返してガスを払い直していた。

---

## 【自分の失敗】V3検証で価格表を捨てていたのは間違いだった(2026年9月21日、即日修正)

昨日入れた「ずれが大きい価格表を捨てる」(`VERIFY_DROP_TABLE_BPS=50`)を、
オーナーの指示で詳しく調べたところ、**3つの欠陥**が出た。**全部「機会を減らす」方向**。

### 実測(本番ログ、10分間に2件とも取り下げ)

```
[V3検証] avalanche 0xd18384F4…(1.00%) 投入$200: 補間が公式より過小28.3bps
[V3検証] avalanche 0xd18384F4…(1.00%) 投入$700: 補間が公式より過小92.2bps
[V3検証] avalanche 0xd18384F4…(1.00%): ずれ92.2bpsは大きすぎるため価格表を捨てました
[V3検証] avalanche 0x27b571f3…(0.30%) 投入$200: 補間が公式より過小84.8bps
[V3検証] avalanche 0x27b571f3…(0.30%): ずれ84.8bpsは大きすぎるため価格表を捨てました
```

### 欠陥①: 使わない額のずれで捨てていた

確かめていたのは **$20 / $200 / $700**。だが **実際の取引額は $1〜30**。

取り下げた2件とも **$20 では誤差20bps以内**(閾値以下なのでログにも出ていない)。
**$200 と $700 のずれだけを理由に、使える額のプールを地図から消していた。**

### 欠陥②: 向きを区別していなかった

2件とも「**補間が公式より過小**」= こちらの見積もりが**辛い**側。

| ずれの向き | 何が起きるか | 危険か |
|---|---|---|
| **過大** | 幻の利益を見る → 送って赤字/無駄な確認 | **危険** |
| **過小** | 実際より少なく見積もる → 機会を取り逃す | **損はしない** |

**安全な側のずれで、プールを消していた。** しかも消せば機会はもっと減る。

### 欠陥③: 測っていない向きの表まで消していた

検証は `zeroForOne: true` しか測っていないのに、`clearQuoteTable` は**両方向**を消していた。
**確かめてもいない理由で、逆向きの経路まで判定不能にしていた。**

### そして根本の問題: 捨ててもずれは直らない

作り直しても**同じ形の補間**なので、また捨てることになる(堂々巡り)。
その間、そのプールを通る経路は**1本も判定されない**
(`legIsUsable` が価格表を要求するため)。

### 直し方: 消すのではなく、**信用できる範囲まで投入量を抑える**

その仕組みは**既にあった**。`routeMaxAmountIn` → `getTableRange().max` が
経路の投入量を価格表の範囲で頭打ちにしている。ここに「信用できる上限」を足すだけ。

- 小さい順に確かめ、**過大が出た手前の額**を「信用できる上限」にする
- **過小では制限しない**(損にならないので、制限すると機会が減るだけ)
- いちばん小さい額でも過大なら、**その向きだけ**取り下げる
- 全部通れば、前に付けた制限を**外す**(流動性が改善したら戻れる)
- 確かめる額に **$5** を足した(実際の取引額を含む点を必ず入れる)

検算(実測値をそのまま入れた):

```
✓ avalanche 0xd18384F4 実測(過小) → 制限なし(全部信用できる)
✓ avalanche 0x27b571f3 実測(過小) → 制限なし(全部信用できる)
✓ 過大が$200で出た                 → 上限を$20に制限
✓ 最小でも過大                     → その向きだけ捨てる
```

**今日取り下げた2件は、新しい判定では両方とも残る。**

生存ログ: `V3表[確認N 上限制限M 取下K 過小L]`(どれも0なら出ない)

### 教訓

> **「測って出すだけ」を「使う」に変える時は、使い方を間違えると害になる。**

昨日の私は「測っているのに使っていない値」を見つけて直したつもりだった。
だが**使い方が乱暴**で、測っていない条件(実際の取引額)と、
測っていない対象(逆向きの表)まで巻き込んでいた。

> **閾値を絶対値で判定する前に、符号に意味がないかを確かめること。**
> `Math.abs(bps) > 50` と書いた時点で、「過大」と「過小」を同じ扱いにしていた。
> この2つは**まったく逆の結果**をもたらす。

---

## 【重大】競争に負けたプールを、永久に地図から消していた(2026年9月21日)

V3検証の1件を直して終わりにしかけたところ、オーナーから
「1つ直して満足してはいけない」と指摘を受け、**同じ型の欠陥を系統的に洗った**。
出てきたのは、今日いちばん重い欠陥だった。

### 何が起きていたか(3つが連鎖していた)

**① `wait` の失敗を「壊れたプール」として数えていた**

```js
throw new ExecutionError(`確定待ちで失敗: …`, { reverted: true, stage: "wait" });
```

`wait` は**送信した後に価格が動いて取り消された**状態。
「誰が取ったか」の集計で、原因が **他者の裁定 / 通常取引** だと実測できている。
**つまり他者に先を越された時に必ず起きる、正常な負け方。**

それを `送信失敗N回(価格が信用できない)` として数えていた。

**② 3回で永久追放。しかも回数が減らない**

```js
const DISABLE_AFTER_FAILURES = 3;
const poolFailures = new Map();   // 減らない。期限も無い
```

`disablePool` は `disabledPools` に入れ、`incompatible-pools.json` にも書く。
**解除する道がどこにも無い。**

**→ 競争が激しいプールほど早く消える。大きな機会があるのはまさにそういう
プールなので、取りたい場所から順に地図から消していた。**

**③ 私の朝の修正が、これを悪化させていた**

同じ日の朝、`incompatible-pools.json` を `/tmp` からボリュームへ移した
(「学んだことが再デプロイで消える」の修正)。

**それまでは再デプロイのたびにこの誤判定が消えていた。**
私の修正で、**誤判定が本当に永久になった。**

> **正しい修正が、別の欠陥と組み合わさって害になることがある。**
> 永続化する前に、**永続化してよい中身かを確かめるべきだった。**

### 直し方

| | 前 | 後 |
|---|---|---|
| `wait`(先を越された) | 失敗として数える | **数えない**(冷却だけ)。別枠で数える |
| 失敗回数 | 減らない | **30分の窓**。窓の外は忘れる |
| 3回に達したら | **永久追放** | **60分だけ外す**。期限が来たら戻る |
| 永久にしてよい理由 | 文面で判定 | **呼ぶ側が `permanent` で明示** |
| 既存の記録 | そのまま | **`送信失敗…` の記録を起動時に解除** |

### 文面で判定してはいけない(自分の検算で捕まえた)

最初は理由の文字列で `/税トークン|詐欺トークン|異常なリターン/` と書いた。
検算したところ:

```
✗ 一時 ← 送金時に税を取るトークン(手数料500bps超)
```

**「税を取るトークン」は「税トークン」に一致しない。**
税トークンが一時扱いになる、逆向きの欠陥を作っていた。

日本語の文面はいつでも書き換わる。**呼ぶ側が明示する形に変えた。**
既存ファイルの掃除だけは文面に頼らざるを得ないが、そこは
**「残すもの」ではなく「落とすもの」(`/^送信失敗/`)を名指し**にした。
残す側を名指しにすると、書き換えた時に取りこぼす。

### 検算

```
✓ 他者に先を越された3回(wait)     数えた失敗0回 → 外さない
✓ 本当の失敗3回が30分以内         数えた失敗3回 → 一時的に外す(60分)
✓ 本当の失敗3回が窓をまたぐ       数えた失敗3回 → 外さない
✓ waitに本物が2回混ざる           数えた失敗2回 → 外さない
```

生存ログ: `外した[永久N 一時M 先越されK]`

### 教訓

> **「失敗」と「負け」を同じ箱に入れないこと。**
> 壊れているから取れなかったのか、他人に先を越されたのかは、まったく別の話。
> 前者はプールの問題、後者は**機会があった証拠**。

> **取り消せない処置を入れる時は、戻る道を同時に作ること。**
> 一時除外(`notePoolBlame`)には期限があったのに、
> こちらには無かった。同じ役目の仕組みが2つあって、片方だけ雑だった。

---

## 記録簿の見出しの8割が幻だった + Base の確認失敗の正体を追えるようにした(2026年9月21日)

オーナーから画面の指摘:「ここに書かれてる利益を取れるようにするか、
取れるようにならず取引にも活かされないなら要らない」。**もっともな指摘。**

### ① 見出しの8割が幻だった

画面の「黒字判定の合計 **+$32.979**」の上位4件:

| 経路 | 理由 | 投入 | 判定額 | 利回り |
|---|---|---|---|---|
| polygon uniswap→sync発見→v3(0.05%) | 罠の疑い | $0.59 | **+$18.76** | **3,180%** |
| 同上 | 罠の疑い | $0.59 | +$4.29 | 727% |
| 同上 | 罠の疑い | $0.59 | +$1.49 | 253% |
| 同上 | 罠の疑い | $0.59 | +$1.47 | 250% |

**この4件だけで $26.02。** 実データで検算すると、上位の **95%が幻**だった。

「$33を取り逃している」と読めてしまい、**本当に直すべきもの
(simulate 失敗の $0.94)が埋もれる。**

**直した。** 記録簿を3つに分ける。

| 出すもの | 意味 |
|---|---|
| **実際に得た利益** | 手元に残った額 |
| **取れた可能性がある額** | 幻を除いた取り逃し。**これが本当の数字** |
| **計算が壊れた経路** | 有り得ない利回りの判定。**金額ではなく不具合の一覧** |

さらに **「直せば取れる額(原因べつ)」** の表を足した。金額の大きい順に、
原因と**直し方**を並べる。これが「次に何を直すか」そのものになる。

```
送信失敗      2件  +$0.9391  取り消しの中身(セレクタ)から原因を特定する
送信直前に見送り 1件  +$0.4833  判定と実測のずれ。価格表の精度か、反応の遅れ
```

### ② Base の確認失敗は、再デプロイでは直っていなかった

大物の台帳を入れた直後、**30分で答えが出た**。

```
[実行] 失敗(simulate): execution reverted (unknown custom error)
[大物/逃した] base uniswap-v3(0.30%)→aerodrome→sync発見: 見込み$0.2294(投入$18.40) → 送信して失敗(simulate)
[大物/逃した] base uniswap-v3(0.05%)→aerodrome→sync発見: 見込み$0.2583(投入$23.92) → 送信して失敗(simulate)
[大物/逃した] base uniswap-v3(1.00%)→aerodrome→sync発見: 見込み$0.1246(投入$16.00) → 送信して失敗(simulate)
[大物] 30分: $0.10以上を 4件検知 → 成立0件 / 逃した4件(見込み$0.8418)[送信して失敗:3 冷却中:1]
```

**朝と同じ3本の経路**(`uniswap-v3(X%)→aerodrome→sync発見`)。
コントラクトを再デプロイしても直っていない。ただし**症状は変わった**:
「missing revert data」→「**unknown custom error**」。
つまり取り消しの中身は返るようになったが、**読めていない**。

**そして今までのコードは、読めなかった中身を捨てていた。**
原因究明に一番必要な情報(セレクタ)を、自分で消していた。

**直した。** 読めない時も**必ずセレクタを残す**。よくある取り消しの照合表も付けた
(`ethers.id("K()")` などで計算して確認済み)。

| セレクタ | 意味 |
|---|---|
| `0xebb6e92f` | `SimulationResult` — うちの確認結果 |
| `0xa932492f` | `K()` — **Solidly系(Aerodrome等)。受取量が多すぎる** |
| `0x42301c23` | `InsufficientOutputAmount()` |
| `0xbb55fd27` | `InsufficientLiquidity()` |
| `0xa1bf7886` | `LOK()` — Uniswap V3 が未初期化/ロック中 |

**3本とも `→aerodrome→` を通っている**ので、Aerodrome の手数料の見立てが
違っていて `K()` が返っている、というのが今の見立て。**次のログで確定する。**

### ③ 自分の不具合を1件

大物のまとめが、チェーンごとの繰り返しの中にあったため**同じ行が5〜6回**流れていた。
繰り返しの外へ出した。

### 教訓

> **合計を出す時は、「足してよいもの」かを先に決めること。**
> 有り得ない値を混ぜた合計は、数字が大きいぶんだけ判断を狂わせる。

> **読めなかったものを捨てない。** 「unknown」は答えではなく、
> **生データを残していれば答えになったはずのもの**。

---

## 正しい処理は最初から書いてあったのに、一度も発動していなかった(2026年9月21日)

セレクタを残すようにした直後、**1分で答えが出た**。

```
[実行] 失敗(simulate): execution reverted (unknown custom error)
       / 取り消しの中身: 0xa932492f = K() — Solidly系(Aerodrome等)。**受取量が多すぎる**
```

**見立てどおり `K()`。** Aerodrome が「その受取量では不変量を割る」と拒否していた。
= **こちらの手数料の前提が実際より低く、出せない量を要求していた。**

### そして、それを直す処理は最初から書いてあった

```js
if (isKRevert(msg)) {
  markRouteRejected(opp);   // → 手数料の前提を外して実測し直す
  throw new ExecutionError(`V2の手数料の実測値が実際より低い(K検算で拒否)…`);
}
```

**なぜ発動しなかったか。** `isKRevert` が**文字列しか見ていなかった**。

```js
return /: K\b/.test(m) || /["']K["']/.test(m);
```

`require(..., 'K')` を使う**古い** Uniswap V2 フォークなら `reverted: K` が返るので
引っかかる。しかし **Aerodrome / Velodrome は custom error `K()`** を使う。
これは文字列を一切返さず、`execution reverted (unknown custom error)` としか出ない。

**だからこの判定は一度も true にならなかった。**

結果、Base の `uniswap-v3(X%)→aerodrome→sync発見` が見込み **$0.12〜$0.49**
(今の平均の20〜50倍)で**何度も落ち続けていた**のに、手数料の見直しに繋がらず、
ただの「確認失敗」として捨てられていた。

### 直した

- **セレクタで見分ける**(`0xa932492f` = `K()`、`0x438d3ade` = `BelowMinimumK()`)。
  `ethers.id("K()").slice(0,10)` で計算して確認済み
- **1回で手数料の前提を外す**(`forceFeeReprobe`)。赤字は「惜しかった」可能性が
  あるので2回待つが、**K検算での拒否は計算が物理的に不可能な証拠**なので待たない
- 一時無効のログで理由を70文字で切っていたため、**いちばん知りたいセレクタが
  消えていた**。140文字に広げた

検算(本番で出た実物を含む):

```
✓ K検算と判定   ← 本番で出た実物(Aerodrome custom error)
✓ K検算と判定   ← 旧 Uniswap V2 フォーク(文字列)
✓ K検算ではない ← うちの確認結果(SimulationResult)
```

### あわせて確認できたこと

同じログに、1時間前に入れた修正が**正しく効いている**証拠が出ていた。

```
[一時無効] base 0x7ef36ca3…: 送信失敗3回(30分以内) → 60分だけ外します(永久ではありません)
```

**この2つのプールは、朝の版なら永久追放されていた。**
そして**それこそが $0.12〜$0.49 の機会を運んでいるプール**だった。

### 教訓

> **「判定が false を返し続けている」は、「条件が起きていない」とは限らない。**
> 起きているのに**見分けられていない**場合がある。
> 分岐が一度も通らない時は、条件そのものを疑うこと。

> **文字列で外部の失敗を見分けない。** 相手は実装を変える。
> `require(..., 'K')` から `error K()` への移行は、まさにそれが起きた例。
> **セレクタ(4バイト)は変わらない。**

---

## 進行中の計画
1. 済: V3型プールをDEX別に数える調査(scripts/pool-survey.js)。Polygon /
   Base / Optimism で実施し、未監視ファクトリーの活動量と、監視ペアの
   プールの有無・手数料・流動性を把握した
2. フォークのプールを見積もれるようにする(上の節を参照)。次の順で進める
   - 済: コントラクトに quoteV3 を追加(コンパイル確認まで)
   - 済: bot側で「公式で求まらなかった分だけ」自前に回す仕組み
   - 未: 再デプロイ。オーナーの立ち会いのもとで行う。デプロイ前に
     コントラクトの中身を確認し、旧コントラクトの利益を withdraw で回収する
   - 済: 見つかったファクトリーを V3_FACTORIES に追加(fork: true 付き)。
     Algebra系は手数料帯を取らないため poolByPair で探す。ENABLE_FORK_QUOTER
     が未設定の間は探索対象にならないので、本番の動きは変わらない
   - 済: 再デプロイ(Polygon、2026年9月18日朝)。デプロイ前にコントラクトを
     確認し、quoting が実取引に漏れないことを検証した(下の節)
   - 済: ENABLE_FORK_QUOTER=polygon で効果を確認(下の節)
   - 済: Optimism へ展開(2026年9月19日に再デプロイ)
   - 済: Avalanche / Arbitrum へ展開(2026年9月20日、ガス削減版で再デプロイ)。
     ENABLE_FORK_QUOTER に avalanche を追加し、Pangolin V3(fork-e)と Pharaoh の探索を開始
3. 対象チェーンの優先順位は Polygon(31.5%が未監視)→ Optimism(ガス最安・
   壁0.06%・27プール)→ Avalanche(82%が未監視。ガス$0.001で最安)→
   Base(活動量は最大だが主要DEXの出来高が対象外トークンに偏る)
4. 済: Algebra対応(globalState と Swap識別子)。上の節を参照。
   これで Polygon の algebra-a(V3 Swapの22.4%)が判定に入る

5. 【2026年9月21日に追加】戦略の見直し(オーナー了承済み。順に進める)

   ### 前提として確定した事実(外部研究 + 自前の実測)
   - 同一チェーン内の原子的裁定は **$1〜5/日が天井**。深い経路は壁3〜6bpsでも
     −6〜−7bps(`[試算] 最良の1本`)。価格差そのものが無い
   - 理由:Base / Optimism ではガスの51〜55%を裁定契約の「探り」が消費し、
     取引に至るのは6〜12%(ETH Zurich, Optimistic MEV, AFT 2025)。専業は
     **反応せず、毎ブロック取引を送って列に並び続けている**。反応時間ゼロの
     相手には、どれだけ速くしても反応型では勝てない
   - 自前の実測では赤字の原因は「他者の裁定0件 / 通常取引で動いた12件」。
     浅い帯(投入$1〜10)では競合がおらず、ここが今の稼ぎ場
   - Arbitrum Timeboost は3社で落札の99.74%。却下
   - 2チェーン間の非原子的裁定は、価格のズレが **10〜20ブロック(20〜40秒)持続**
     し、Arbitrum / Base / Optimism で4か月に50万件超(UZH / Matter Labs,
     arXiv 2406.02172)。速さではなく在庫の戦い。在庫(ETH)の価格リスクを取る
   - Aave V3 の清算は上位5者で46%だが大口の話。L2 の $100〜1,000 の小口は
     放置されている帯。flash loan で元手不要。荒れる日に集中する
     (2026年1月31日〜2月5日:5日で12,500件・$4.29億)

   ### 段階1:反応の遅れを削る(費用ほぼゼロ。今の戦略・B・C の全部に効く)
   実測のループは **約600〜800ms = Flashblock 3〜4個ぶん遅い**。内訳は
   Flashblocks の取得遅れ平均200ms(400ms ごとの取りに行き)+ 送信前の確認
   85〜100ms + ガス見積もり〜送信準備 177〜451ms + Railway(sfo)→米東海岸の
   シーケンサー 60〜80ms。目標は **100〜150ms**。
   - 未: 送信前の確認(eth_call)とガス見積もりを省く。コントラクトの minProfit
     検算が同じことをしているので二重。ガス上限は実測の中央値で固定。
     空振りは Optimism で $0.005
   - 未: Flashblocks を 400ms ごとの取りに行きから WebSocket の押し出しに
     (Base は公開の Flashblocks WS がある。要確認)
   - 未: シーケンサーの受付URLへ直接送る(Base / Optimism / Arbitrum は公開。
     読むのは今まで通り Chainstack)
   - 未: Railway を US East へ(ボリュームの引っ越しを伴う。オーナー作業)

   ### 段階1.5:「原因」で撃つ(測定から入る。リスクゼロ)
   チェーンの中に専業より早い情報は無い(公開メモリプールが無く、シーケンサーが
   最初の観測者)。しかし専業が反応する「被害者の取引」の**原因**はチェーンの外で
   数秒早く見える:取引所(Binance / Coinbase)の価格、UniswapX / CoW の公開注文、
   Chainlink の乖離閾値。専業は結果に反応し、こちらは原因に反応する。
   - 未: **まず測る。** Binance の公開 WebSocket(ETH/USDT)を1本つなぎ、
     「取引所が Xbps 動いてから、監視プールが動くまで何秒か」と
     「どのプールが最初に反応し、どれが最後まで残るか」「動く時間帯」を記録する。
     送信は一切変えない。1〜2日ぶん貯める
   - 未(測定の結果次第): 取引所が動いた瞬間に、確認なしで裁定を送る
     「原因で撃つ」。空振りは minProfit の revert でガス代のみ。
     専業の「常時探り」($216〜10,800/日)を「根拠のある時だけ探る」に絞った形

   ### 段階2以降(利益が見えてから判断)
   - A: Base を再開(上の「再開手順」。Flashblocks は Chainstack の pending で
     そのまま動く。出来高は Optimism の約10倍)。期待は回数のみ($1〜5/日)
   - C: Aave 小口清算。**設計書を `docs/AAVE-LIQUIDATION-PLAN.md` に作成済み
     (2026年9月21日)。** 仕様は一次情報で確認済み(liquidationCall の引数、
     close factor 50%/100% の境目 HF<0.95、healthFactor は18桁、各チェーンの
     Pool 住所)。いまのコントラクトに**清算の1手を差し込むだけ**で足りる。
     第1段は**読み取りだけ(再デプロイ不要・リスクゼロ)**で、1日に何件・
     いくらの機会があるかを実測する。見合わなければそこで止める。
   - B: 2チェーン間の非原子的裁定。$2,000〜5,000 × 2チェーンの在庫。
     オーナーが ETH の価格リスクを許容した場合のみ
   - D: UniswapX filler(Arbitrum は RFQ も独占期間も無く誰でも即埋められる)。
     B / C の後
   - 段階2: 自前ノード(米東海岸)+ ローカル計算。$200〜500/月

## 後回しにした対策
- 速度: ガス見積もり省略、Arbitrumシーケンサー直結送信とフィード購読、RailwayをUS Eastへ、Chainstack Trader Node
  (nonceの事前取得は「速度」ではなく機会を失う不具合だったため対応済み)
- 範囲: 始点トークン拡張、4段経路、BNB追加、Polygon FastLane入札
- 利益: 投入額の最適化、取引上限の引き上げ(コントラクトのガス削減は2026年9月20日に実装済み、再デプロイ待ち)
- 守り: 私設送信、コントラクトからの利益の引き出し、異常の通知

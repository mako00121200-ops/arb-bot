**作業前に必ず docs/HANDOVER.md を読むこと。**

# arb-bot 作業ルール

- 返答・コミットメッセージ・コード内のコメントは日本語で書く
- 秘密鍵・APIキー・RPCのURLなどの秘密情報は、リポジトリに絶対に書かない(Railwayの環境変数で管理している)
- 本番で稼働中のDEXアービトラージbot。変更は依頼された範囲だけに留め、無関係なファイルは触らない
- ESモジュール(import/export)で書く
- 変更後は `node --check` で構文を確認する
- `contracts/` のSolidityを変えた場合は、その旨を明記する(再デプロイが必要になるため)
